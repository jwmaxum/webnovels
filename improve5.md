# 5단계 — 원고 자동저장·복구·동시 편집 안정화

[전체 계획](improve.md) · 이전: [4단계](improve4.md) · 다음: [6단계](improve6.md)

## 목표와 선행 조건

자기 작품을 선택해 원고를 작성하고, 저장 실패·화면 이동·두 기기 편집에서도 원고를 보존한다. 2단계 초안 모델과 3단계 권한 API를 사용한다.

## 대상 파일

- `public/js/creator/creator-editor.js`: `dbRequest`, `loadDraft`, `saveLocal`, `syncServer`, `clearCurrentDraft`.
- `public/js/creator/creator.js`: `prepareNewEpisodeForWork`와 발행 후 초기화 연결.
- `public/supabase-admin.js`: 기존 초안 직접 upsert 대체.
- `public/index.html`, `public/styles.css`, 새 초안 API/DB 트랜잭션.

## 순차 작업 (로컬 구현)

- [x] 현재 원고 스냅샷·안정적인 draft ID·수정 버전·로컬 저장 버전·서버 저장 버전을 별도 상태로 관리한다.
- [x] 작품/회차 선택 변경 전 이전 원고를 이전 ID로 저장한다. 저장 타이머와 비동기 응답을 해당 초안에 고정한다.
- [x] `prepareNewEpisodeForWork()`의 프로그램 선택 변경도 같은 전환 함수를 거치게 한다.
- [x] 새 초안에 복구 데이터가 없으면 입력을 비운다. 새 로드가 끝난 후 이전 응답이 폼을 덮어쓰지 못하게 한다.
- [x] 로컬 IndexedDB 저장은 transaction 완료 후 성공 표시한다. abort·quota 오류를 사용자에게 알리고 다운로드를 제공한다.
- [x] 입력 debounce 저장·화면 전환 저장·재연결 동기화를 공통화한다. beforeunload만을 유일한 보존 수단으로 사용하지 않는다.
- [x] 서버 저장은 기준 revision 조건부 갱신으로 원자 처리한다. 두 기기/두 탭 충돌에 양쪽 원고 사본·비교·복구를 제공한다.
- [x] 서버 응답 시 이후 입력까지 저장됐다고 표시하지 않는다. 최신 미저장 내용은 다음 동기화 대상으로 남긴다.
- [x] 마지막 수정 시각만으로 원격/로컬 원고를 자동 덮어쓰지 않는다. 기기 시계 차이와 충돌을 고려한다.
- [x] 기존 로컬 키(작가:작품:회차) 초안은 삭제하지 않고 새 ID로 이관한다. 계정 소유를 확인할 수 없는 원고는 임의 계정에 합치지 않는다.
- [x] 기존 Diff·버전 복구·서식 도구를 유지한다. 복구/서식 변경 직전 백업과 실행 취소를 보장한다.
- [ ] 발행 완료 초안은 게시 회차와 연결해 보관 정책으로 처리한다. 다음 회차 진입 시 새 키가 만들어지게 하고 7단계에서 통합 검증한다.
- [x] 로그아웃/계정 전환 시 화면·타이머·메모리 캐시를 정리한다. 미동기 원고는 알림·백업 없이 버리지 않는다.
- [x] 모바일 집중 편집, 한글 조합 입력, 키보드 가림, 키보드 단축키·포커스·저장 상태 알림을 지원한다.

## 검증과 완료 조건 (로컬 대역·합성 DB 기준)

- [x] A 작품 작성 → B 작품 전환 → A 복귀에서 양쪽 원고가 분리 보존된다.
- [x] 마지막 입력 직후 전환·새로고침·오프라인 복귀에서 저장 범위가 정확하다.
- [x] 응답 순서를 역전해도 오래된 데이터가 최신 내용을 덮어쓰지 않는다.
- [x] 두 기기/두 탭의 동시 저장이 조용히 덮어쓰이지 않고 복구 가능하다.
- [x] 저장공간 부족·세션 만료·네트워크 오류가 성공으로 표시되지 않는다.
- [x] 사용자 B에게 사용자 A의 초안·로컬 버전이 표시되지 않는다.
- [x] 버전 복원 전 원고도 다시 복구 가능하다. 모의 테스트와 실제 브라우저 확인 여부를 구분한다.

## 복구·주의 사항

IndexedDB 버전 이관 실패 시 기존 저장소를 삭제하지 않는다. 원본을 보존하고 읽기/내보내기 경로를 제공한다. 새로운 편집기로 교체하더라도 원고 이관 검증을 먼저 수행한다.

## 실행 기록

- 상태: **로컬 구현·검증 완료 / 실제 DB·브라우저 통합 검증 대기**
- 시작일 / 로컬 검증일: 2026-09-23 / 2026-09-23. 단계 전체 완료일은 미정.
- 변경: 원고 상태 엔진·IndexedDB v2·편집 UI, 고정 UUID/탭별 사본, 조건부 revision 및 멱등 저장 API·006 SQL, 충돌 비교/복구/서식 취소, 세션 정리, 기존 upsert·자동 발행 제거, 테스트·CI.
- 상세: [원고 저장·복구 계약](docs/launch/creator-drafts-contract.md).
- 검증: `npm run test:drafts` 총 23개(engine 9, IDB 대역/이관/Diff 4, UI VM 3, HTTP mock 4, PGlite 3), Auth 9, 기존 API 26, Cloudflare 4, 작품 API/UI/DB 17, 데이터 계약·타입/구문 검사.
- 증거: [step5-draft-verification.json](artifacts/step5-draft-verification.json). 브라우저/실기기 테스트가 아닌 대역·합성 DB 검증이다.
- DB/Storage: 006 증분 SQL 작성·합성 DB 검증만 수행. 실제 Supabase 미적용. `AUTHOR_DRAFTS_ENABLED=false` 기본값 유지.
- 발행 보관: 물리 삭제 제거, PUBLISHED/episode_id 읽기 및 읽기 전용 처리, 새 UUID 준비 완료. 실제 발행 트랜잭션 연결은 7단계에서 구현·통합 검증.
- 미해결 인수: 2단계 실제 스키마·백업·Auth 매핑, 3단계 실제 메일/세션, 브라우저 IndexedDB 업그레이드·용량 부족·다중 탭·모바일 IME/키보드·새로고침 인수. 관리 API 401 해소 여부는 이번 단계에서 새로 확인하지 않았다.
- 커밋/배포: 수행하지 않음. 기존 작업 트리 변경을 보존함.
- 다음 단계: 6단계 로컬 개발 가능. 운영 활성화와 서비스 오픈 판정은 대기.
