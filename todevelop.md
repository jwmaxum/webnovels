# 작가 친화 기능 고도화 개발 계획서

## 1. 판단 결과

제안은 **적합하며, 작가 유치·연재 지속률·독자 완독률을 함께 높이는 방향**이다. 특히 현재 서비스가 이미 보유한 작가 스튜디오, 예약 발행, 광고 수익 배분, 회차 댓글, 작품 태그, 기본 리더 설정과 자연스럽게 이어진다.

다만 아래처럼 우선순위를 조정해 진행한다.

| 판단 | 항목 | 근거 및 처리 방향 |
| --- | --- | --- |
| 즉시 도입 | 자동 임시저장, 글자 수, 모바일 집필 UX, 리더 개인화 | 현재 회차 작성 화면은 단순 `textarea`이며, 리더는 테마 3종과 글자 크기만 지원한다. 작가/독자의 핵심 사용 흐름을 직접 개선한다. |
| 2차 도입 | 세부 태그·검색 필터, 신인 노출, 문단 댓글, 댓글 안전 도구 | `works.tags`, 회차 댓글 및 신고 기반이 있으므로 확장이 가능하다. 다만 문단 댓글은 본문 버전과 위치를 안정적으로 식별해야 한다. |
| 3차 도입 | 회차별 수익 상세, 후원, 완독/이탈 분석 | 현 수익 대시보드와 독서 진행 저장의 기반은 있으나, 금융 원장·부정 사용 방지·개인정보 집계 기준을 먼저 갖춰야 한다. |
| 별도 정책/검토 후 도입 | AI 교정·설정 충돌, 정기 멤버십, 굿즈/IP 선언 | 외부 AI 비용·개인정보·저작권, 결제/환불/세무, 서비스 약관 변경이 수반된다. MVP 범위에 바로 넣지 않는다. |

### 현재 구현 기준 진단

- 작가 회차 작성은 `public/index.html`의 `#newEpContent` 단일 textarea와 `public/js/creator/creator.js`의 제출 처리로 구성되어 있다. 글자 수, 초안 복구, 버전 이력, 문단 서식은 없다.
- 리더는 `public/js/reader/reader.js`에서 Light/Sepia/Dark 테마와 14~26px 글자 크기를 저장하지만, 글꼴·줄간격·여백·올레드 블랙 설정은 없다.
- `works.tags`와 제목/소개/태그 검색은 존재하지만, 태그 표준화·복수 필터·회차 수·완결 조건은 없다.
- 회차 댓글과 좋아요/신고는 있으나, 댓글은 회차에만 연결되어 있고 문단 앵커·스포일러·작가별 필터 규칙이 없다.
- 수익 대시보드와 독서 진행 저장은 존재하지만, 회차별 광고 노출/적립 원장과 독자 이탈 이벤트는 부족하다.
- Prisma 스키마와 `database/*.sql`이 병존하며 필드와 테이블 설계가 일부 다르다. 신규 기능의 단일 데이터 모델을 먼저 확정해야 한다.

## 2. 목표와 비목표

### 목표

1. 작가가 네트워크·브라우저 오류에도 원고를 잃지 않고 PC와 모바일에서 회차를 작성·복구·발행한다.
2. 독자가 자신의 읽기 환경을 저장하고, 안전하게 특정 문단에 반응한다.
3. 신인 작품도 일관되고 설명 가능한 기준으로 노출되며, 작가는 독자 반응과 수익의 근거를 확인한다.
4. 모든 금전·노출·분석 기능은 감사 가능하고 조작에 강한 서버 기준 데이터로 제공한다.

### 이번 로드맵의 비목표

- AI가 원고를 임의로 수정하거나 출판 적합성을 자동 판정하는 기능
- 실제 결제수단을 통한 정기 멤버십/후원 출시
- 대외 법률 자문 없이 IP 비독점 조항 또는 수익 정산 약관을 변경하는 작업

## 3. 선행 단계: 데이터·권한 기준 통합

### 3.1 설계 결정

1. 운영 데이터의 기준을 Prisma migration으로 통일하고, 기존 SQL은 초기 배포·참조용으로 역할을 분리한다.
2. 기존 `author`/`creator` 용어는 API 응답 호환성을 유지하면서 내부 도메인 명칭을 `creator`로 수렴한다.
3. 금액은 부동소수점이 아닌 최소 화폐 단위 정수 또는 고정소수점으로 저장한다.
4. 사용자별 설정·초안·원고는 소유자만 접근하도록 인증 미들웨어와 DB 정책을 함께 적용한다.

### 3.2 해야 할 일

1. 현재 Prisma 모델과 `database/03_content.sql`~`17_creators_view.sql`의 엔터티/필드/권한 차이를 표로 만들고, 데이터 이전 규칙을 확정한다.
2. migration 도입 방식과 배포 순서(확장 → 백필 → 코드 전환 → 구 경로 제거)를 문서화한다.
3. 기존 데모 데이터와 실제 API 응답을 분리한다. 작가 스튜디오와 리더가 `SAMPLE_WORKS`를 fallback으로 쓰는 부분은 개발 모드로 한정한다.
4. 소유권 검증, 관리자 권한, 감사 로그, 데이터 보존 기간의 공통 정책을 정의한다.

### 완료 기준

- 스키마 변경이 Prisma migration 한 경로로 재현된다.
- 작가 A가 작가 B의 초안·통계·정산 세부 내역에 접근할 수 없다.
- 스테이징 데이터에서 기존 작품/회차/댓글/수익 요약이 유실 없이 이전된다.

## 4. 1단계 — Zero-Stress Creator Suite (최우선)

### 4.1 회차 초안 및 이중 저장

#### 데이터 모델

- `episode_drafts`: `id`, `creator_id`, `work_id`, `episode_id(nullable)`, `title`, `content`, `author_comment`, `content_format_version`, `local_revision`, `server_revision`, `updated_at`, `deleted_at`.
- `episode_draft_revisions`: 초안 저장 시점별 스냅샷, 생성자, 변경 시각, 복구 원본 식별자를 저장한다.
- 브라우저 IndexedDB에는 사용자·작품·회차 키, 수정 시각, 서버 revision, payload checksum을 저장한다. `localStorage`는 대용량 장문 원고 저장소로 사용하지 않는다.

#### 사용자 흐름

1. 작가가 작품과 회차를 선택하거나 새 회차를 열면 서버 초안과 IndexedDB 초안을 비교한다.
2. 더 최신 초안이 있으면 “로컬 초안 복구 / 서버 초안 사용 / 비교 후 선택” 화면을 표시한다.
3. 입력 후 1초 debounce로 IndexedDB에 저장하고, 네트워크가 정상일 때 60초마다 또는 blur/발행 직전에 서버로 동기화한다.
4. 동시 편집 충돌은 server revision 비교로 감지하고, 자동 덮어쓰기 대신 병합 선택 화면을 제공한다.
5. 발행 성공 후 해당 초안을 읽기 전용 revision으로 보관하고, 로컬 초안은 명시적 확인 후 정리한다.

#### 구현 단위

1. `src/routes/creator.router.ts`에 초안 생성·조회·갱신·revision 목록·복구 API를 추가한다.
2. `public/js/creator/creator.js`를 `creator-editor.js`와 `creator-draft-store.js`로 분리하고, IndexedDB 저장 어댑터·동기화 큐·오프라인 상태 표시를 구현한다.
3. `public/index.html`의 회차 등록 폼을 작성 상태, 마지막 로컬 저장 시각, 마지막 서버 동기화 시각, 복구, 버전 메뉴를 가진 편집 화면으로 교체한다.
4. 새 회차 발행 API는 클라이언트 회차 번호를 신뢰하지 않고 서버에서 잠금 또는 고유 제약으로 다음 번호를 확정한다.

### 4.2 웹소설 작성 보조와 모바일 UX

1. 제목/본문/작가의 말을 각기 관리하고, 본문에는 공백 포함·제외 글자 수, 문단 수, 권장 4,500~5,500자 진행 게이지를 표시한다.
2. 최소한의 서식은 paragraph 기반으로 제한한다: 첫 줄 들여쓰기, 문단 간격, 대화문 간격, 줄바꿈 정규화. 임의 HTML 입력은 허용하지 않는다.
3. 모바일(폭 768px 이하)은 고정 도구막대, 44px 이상 터치 타깃, 안전영역, 키보드 표시 시 발행 버튼 접근성을 보장한다.
4. 편집 중 화면 이탈/탭 닫기 시 서버 미동기 변경이 있을 때만 경고한다.
5. 접근성: 키보드 단축키, 상태 변화 `aria-live`, 충분한 명도 대비, 자동 저장 실패의 명확한 재시도 버튼을 제공한다.

### 4.3 버전 이력 MVP

1. 자동 저장 revision은 최근 10개 또는 30일을 보관하고, 수동 “버전 저장”은 별도 라벨을 지원한다.
2. revision 비교는 제목·작가의 말·본문을 분리해 diff로 보여 준다.
3. 복구는 새 revision을 생성하는 방식으로 수행하여 복구 작업도 되돌릴 수 있게 한다.

### 4.4 1단계 테스트와 수용 기준

- 6,000자 원고를 입력한 뒤 새로고침/오프라인 전환/브라우저 재시작을 해도 복구 가능하다.
- 네트워크 단절 중 저장된 로컬 원고가 재연결 후 한 번만 서버에 동기화된다.
- 서로 다른 두 탭에서 수정 시 무음 데이터 손실 없이 충돌을 안내한다.
- PC·iOS Safari·Android Chrome에서 편집, 복구, 예약 발행을 확인한다.
- API 단위 테스트(소유권·revision 충돌), 브라우저 E2E(오프라인/복구/발행), 수동 모바일 회귀 테스트를 추가한다.

## 5. 1.5단계 — 리더 개인화와 안전한 본문 렌더링

### 5.1 설정 범위

- 테마: white, sepia, dark, OLED black.
- 글꼴: 명조/고딕 두 계열, 글자 크기, 줄간격, 좌우 여백, 문단 간격.
- 설정 저장: 비로그인은 로컬, 로그인 사용자는 서버 설정과 동기화한다. 사용자가 기기를 바꿔도 복원된다.

### 5.2 구현 단위

1. `reader_preferences`에 사용자의 설정 값과 schema version을 저장하고, API에 조회·부분 갱신을 추가한다.
2. `public/js/reader/reader.js`의 현재 테마/글자 크기 함수를 하나의 preferences store로 통합한다.
3. CSS custom property로 `#readerPaper`의 typography와 여백을 제어하여 인라인 스타일 난립을 줄인다.
4. 현재 `innerHTML`로 본문을 만들기 전에 텍스트를 이스케이프하거나, 허용된 paragraph 구조만 서버에서 생성한다. 독자 주석 기능을 위해 문단별 안정 식별자를 부여한다.

### 수용 기준

- 선택한 설정이 재방문과 다른 기기 로그인 이후에도 유지된다.
- 설정 변경은 즉시 반영되고, 다음 회차 이동 시에도 유지된다.
- 악성 문자열이 본문 또는 작가의 말에서 실행되지 않는다.

## 6. 2단계 — 발견성과 독자 인터랙션

### 6.1 표준 태그·고급 검색

1. `tag_definitions`(표시명, slug, 카테고리, 동의어, 활성 여부)와 `work_tags` 조인 테이블을 도입한다. 자유 입력 태그는 검수 대기 상태를 거친다.
2. 초기 50개 내외의 큐레이션 태그를 운영 도구에서 관리하고, “회귀/빙의/환생”처럼 유사 키워드의 표준 slug를 정한다.
3. 검색 API에 제목·작가 필명·소개·표준 태그, 완결 여부, 최소/최대 회차 수, 정렬, 페이지네이션을 추가한다.
4. 검색 인덱스와 `AND/OR` 태그 필터의 성능 기준을 정하고, 응답에는 적용 필터와 총 건수를 반환한다.
5. 작품 등록/수정 화면에 자동완성 태그 선택, 최대 개수, 금지 태그 검증을 제공한다.

### 6.2 Golden Best 및 신인 노출

1. “최근 24시간 급상승”과 “신규 작가 1~15화”를 분리된 추천 surface로 정의한다.
2. 점수 입력은 서버 이벤트 기반으로 한다: 고유 열람, 완독, 관심 등록, 유효 댓글, 최신 발행, 연재 성실도. 광고 시청/포인트만으로 점수를 부풀리지 않는다.
3. 신인 eligibility, 작품 최소 공개 기간, 작가·작품별 노출 상한, 이상 트래픽 제외, 동점 처리, 갱신 주기를 운영 정책으로 명시한다.
4. 일별 ranking snapshot과 score component를 저장해 “왜 노출됐는지”를 운영자가 감사할 수 있게 한다.
5. 홈과 탐색 화면에는 추천 사유 라벨을 표시한다. 알고리즘 세부 수식과 개인 데이터는 공개하지 않는다.

### 6.3 문단 댓글과 악플 방어

1. `comments`에 `anchor_type`, `paragraph_id`, `quote_text`, `content_version`, `is_spoiler`, `visibility`를 추가한다. 문단 ID는 본문 정규화 뒤 결정적으로 생성한다.
2. 본문 수정으로 앵커를 찾지 못하면 “원문 위치를 찾을 수 없음”으로 표시하고, 임의 문단으로 이동시키지 않는다.
3. 리더에서 문단을 선택하면 짧은 인용과 댓글 입력을 열고, 문단 옆에는 집계 수만 표시한다. 긴 인용문 전체 저장은 최소화한다.
4. 회차 하단 댓글은 계속 지원한다. 문단 댓글/일반 댓글 모두 스포일러 접기, 신고, 좋아요, 차단 상태를 통일한다.
5. 작품 단위 `comment_policy`에 금칙어 목록, 최소 열람 조건, 신규 계정 제한, 작가 숨김/차단 권한을 둔다. 작가 조치는 감사 로그와 이의 제기 경로를 남긴다.

### 2단계 수용 기준

- 여러 태그·완결·회차 수 조건을 조합해 정확히 필터링하고 페이지네이션이 안정적이다.
- 신인 노출이 기존 인기작의 누적 조회수만으로 결정되지 않으며, 계산 근거가 snapshot으로 남는다.
- 문단 댓글은 본문 version과 정확히 연결되고, 스포일러가 기본적으로 노출되지 않는다.
- 작가의 금칙어/열람 자격 규칙은 서버에서 강제되어 클라이언트 우회가 불가능하다.

## 7. 3단계 — 투명 수익·팬덤·분석

### 7.1 회차별 수익 투명성

1. 광고 노출, 검증 완료, 취소/무효, 수익 배분을 append-only `earning_ledger`로 기록한다.
2. 원장은 이벤트 식별자, 작품/회차, 발생 시각, 네트워크, 통화, 총액, 수수료, 작가 배분액, 상태, 정정 원본을 가진다.
3. 대시보드는 일별/작품별/회차별 예상·확정·지급완료 금액을 구분하고, 예상 금액은 추정치임을 명시한다.
4. 관리자의 정산 마감/조정은 사유·담당자·전후 금액을 audit log로 남기며 과거 원장을 수정하지 않고 정정 분개를 추가한다.

### 7.2 후원과 멤버십의 단계적 출시

1. MVP는 플랫폼 포인트 기반의 일회성 후원만 설계한다. 잔액, 원거래, 중복 요청 방지 키, 환불/취소 상태를 transaction으로 처리한다.
2. “오늘의 후원자”는 수신 동의자만 표시하고, 익명 후원/노출 철회 옵션을 제공한다.
3. 정기 멤버십·선공개·디지털 보상은 결제, 청약 철회, 세금계산, 연령 제한, 콘텐츠 접근 만료 정책이 확정된 뒤 별도 프로젝트로 진행한다.

### 7.3 Deep Reader Analytics

1. `reader_events`에 session hash, work/episode, event type(open, paragraph reached, complete, exit), 발생 시각, content version을 기록한다. 원문과 불필요한 개인식별정보는 저장하지 않는다.
2. 완독은 단순 페이지 진입이 아닌 유효 체류시간과 본문 90% 이상 도달을 조합해 정의한다. 백그라운드 탭/봇 이벤트를 제외한다.
3. 집계 테이블로 1→3→5화 퍼널, 회차별 이탈 지점, 공개 후 시간대별 열람, 반응 급증 문단을 산출한다.
4. 작가 화면에는 최소 표본 기준을 넘은 집계만 제공하고, “상관관계이지 원인 확정이 아님”을 표시한다.
5. 개인화 프로파일은 집계 수준으로 제한하며, 연령·성별 같은 민감 추정은 동의와 법적 검토 없이는 제공하지 않는다.

### 3단계 수용 기준

- 작가가 회차별 수익과 해당 수익의 상태/근거를 조회할 수 있고, 총액은 정산 원장과 일치한다.
- 중복 후원 요청이 하나의 거래로만 처리된다.
- 완독률/이탈 그래프는 이벤트 샘플 수와 집계 기간을 함께 보여 주며 테스트 이벤트를 제외한다.

## 8. 별도 후속 프로젝트

### Lorebook·일괄 치환

- 작품별 인물/장소/설정 문서를 `lore_entries`로 관리한다.
- 에디터 사이드바에서 검색·삽입을 제공한다.
- 전체 회차 치환은 preview, 변경 대상 수, revision 생성, 권한 확인, 비동기 작업 큐, 롤백을 갖춘 뒤 제공한다.

### AI 창작 보조

- 맞춤법/띄어쓰기부터 opt-in 방식으로 시작하고, 원고를 외부 모델에 전송할 때의 보관·학습·제3자 제공 여부를 명시한다.
- 설정 충돌은 AI 판단만으로 차단하지 않고 lorebook과 확정 원고를 근거로 한 “검토 제안”으로 표시한다.
- 호출 비용 한도, rate limit, 결과 출처, 오류 시 수동 편집 fallback을 설계한다.

### IP·굿즈·비독점 정책

- 기능 구현 전에 저작권, 동시연재, 콘텐츠 삭제, 수익 배분, 굿즈 판매자 책임, 세무·환불 주체를 법무/사업 담당과 확정한다.
- 정책 확정 후 약관 동의 버전, 작품별 라이선스 선택, 동의 이력, 관리자 승인 흐름을 구현한다.

## 9. 권장 출시 순서와 게이트

| 릴리스 | 범위 | 출시 게이트 |
| --- | --- | --- |
| R0 | 데이터 모델 통합, 권한·감사 기준, API 계약 | migration 리허설 및 권한 보안 검토 통과 |
| R1 | 자동 저장·복구·글자 수·모바일 편집·기본 version history | 오프라인/충돌 E2E, 실제 작가 사용성 테스트 통과 |
| R2 | 리더 개인화와 본문 안전 렌더링 | 모바일 리더 회귀, XSS 검증, 설정 동기화 통과 |
| R3 | 표준 태그·검색·Golden Best MVP·댓글 정책 | 랭킹 부정 사용 검토, 검색 성능, 모더레이션 운영 절차 통과 |
| R4 | 문단 댓글 | 본문 수정 앵커 회귀와 스포일러/신고 E2E 통과 |
| R5 | 수익 원장·일회성 후원·집계 분석 | 금융 reconciliation, 중복 결제 방지, 개인정보 검토 통과 |
| R6 | Lorebook·AI·멤버십·IP/굿즈 | 각 기능의 정책·법무·운영 승인 후 별도 출시 |

## 10. 공통 품질·운영 체크리스트

1. API 입력은 Zod로 검증하고, 출력은 버전 관리된 DTO로 고정한다.
2. 금전/권한/삭제/차단/랭킹 변경에는 idempotency key, 감사 로그, 관리자 알림을 적용한다.
3. 공개 API에는 rate limit과 abuse detection을 적용하고, 댓글/후원/조회 이벤트의 봇 신호를 수집한다.
4. 초안·본문·분석 데이터의 백업, 복구 연습, 보존/파기 정책을 운영 문서에 추가한다.
5. 기능 플래그로 작가 그룹별 제한 베타를 운영하고, 자동 저장 성공률·복구 성공률·발행 전환율·완독률·신인 노출 다양성·신고 처리 시간을 관찰한다.
6. 출시 전에는 `npm run build`, 기존 `npm test`, 신규 API 단위 테스트, Playwright E2E, 모바일 수동 QA, 접근성 점검을 모두 수행한다.

## 11. 주요 위험과 대응

| 위험 | 대응 |
| --- | --- |
| 자동 저장 충돌로 원고 손실 | revision 기반 낙관적 잠금, 복구 UI, append-only revision, 충돌 E2E |
| 문단 수정 후 댓글 위치가 틀어짐 | content version과 paragraph ID 저장, 앵커 미해결 상태를 정직하게 표시 |
| 랭킹 조작/신인 노출 편향 | 고유 사용자·유효 체류 기준, 노출 상한, 이상 탐지, snapshot 감사 |
| 수익 수치 불일치 | immutable ledger, 정정 분개, 일일 reconciliation, 금액 정밀도 통일 |
| 후원/멤버십의 규제·환불 문제 | 포인트 후원 MVP부터, 결제/약관/세무 검토를 정식 출시 게이트로 설정 |
| AI 원고 유출 또는 환각 | 명시적 opt-in, 최소 전송, 보존 정책, 결과를 제안으로만 제공 |

## 12. 착수 전 확정할 제품 결정

1. 초안의 서버 보관 기간과 version history 기본 보관 개수
2. 태그 사전의 운영 주체와 자유 태그 승인 기준
3. Golden Best의 신인 정의, 노출 상한, 갱신 주기
4. 문단 댓글 허용 범위(문단 전체/선택 인용), 최소 열람 조건, 작가 모더레이션 한계
5. 수익 화면에 노출할 추정 단위와 확정/지급 시점
6. 후원 포인트의 환불·수수료·정산 처리 주체
7. 독자 분석에서 동의가 필요한 데이터와 최소 표본 수

이 문서는 구현 계획이며, 코딩은 위 제품 결정을 확정하고 R0부터 승인한 뒤 시작한다.
