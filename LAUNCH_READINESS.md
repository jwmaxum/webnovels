# 서비스 런칭 점검 — 2026-09-21

## 판정

**현재는 외부 고객을 받는 정식 서비스로 런칭할 수 없습니다.**
화면·콘텐츠 관리 기능은 구현되어 있으나, Supabase 권한 및 로그인 신원 검증, 서버 DB 통합, 광고/결제/본인인증 검증이 완성되지 않았습니다. 이번 수정은 데이터 표시 오류와 일부 기능 단절을 개선한 것으로, 보안 및 수익 기능의 운영 완료를 의미하지 않습니다.

## 실제 DB 확인 결과

`npm run audit:live`로 연결된 Supabase 프로젝트를 비로그인 공개 키로 읽기 점검했습니다. 기록은 `artifacts/live-data-audit.json`에 있으며, 개인정보 값과 비밀키는 저장하지 않았습니다.

| 항목 | 확인 결과 |
|---|---:|
| 작품 | 30개 |
| 회차 | 180개 |
| 본문/웹툰 이미지가 모두 비어 있는 회차 | 0개 |
| 독자 / 작가 / 관리자 레코드 | 11 / 30 / 4개 |
| 작가 수익 | 90건 |
| 팬미팅 / 굿즈 / 이벤트 / 광고 단위 | 1 / 2 / 2 / 2건 |
| 비로그인으로 본문이 조회된 유료 회차 | 51개 |

기존 DB에 콘텐츠와 운영 예시 데이터가 이미 있으므로 작품·회차를 중복 생성하지 않았습니다. 화면의 대체 데이터는 제거하고 기존 Supabase 데이터를 사용하도록 수정했습니다. 기존 DB 예시 수익·광고 통계가 실제 매출이라는 검증은 하지 않았습니다. 운영 전 테스트 기록과 실제 거래를 분리해야 합니다.

**이번 작업에서 원격 DB 스키마·RLS 수정이나 신규 시드 INSERT는 실행하지 않았습니다.** 최초 점검에는 서버용 서비스 키가 없었으나, 이후 사용자가 갱신한 공개 키·서버 secret 키·service-role 키는 모두 실제 조회에서 HTTP 200을 확인했습니다. 관리 API 토큰은 재점검에서도 HTTP 401입니다. 데이터 접근 키와 스키마 관리 토큰은 용도가 다르므로 현재 키 갱신만으로 RLS 변경이 완료된 것은 아닙니다.

### 갱신된 환경설정 반영

- `.env.local`의 `NEXT_PUBLIC_SUPABASE_SECRET_KEY`, `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY`를 서버 전용 이름 `SUPABASE_SECRET_KEY`, `SUPABASE_SERVICE_ROLE_KEY`로 변경했습니다. 값은 보존했습니다. `access_token`은 `SUPABASE_ACCESS_TOKEN`으로 이름을 통일했습니다.
- 누락된 `JWT_SECRET`은 임의의 충분히 긴 값으로 생성해 로컬에만 저장했습니다. 서버 설정에 남아 있던 고정 JWT 키도 제거했습니다.
- 배포 환경변수가 최우선이며 로컬에서는 `.env.local`이 `.env`보다 우선합니다. 키 변경 후 서버를 재시작해야 합니다. Vercel 등 배포 환경변수에도 같은 이름으로 등록해야 하며 로컬 파일 수정이 원격 배포 설정까지 바꾸지는 않습니다.
- 브라우저와 관리자 로그인 재시도 경로의 고정 Supabase 키를 제거했습니다. `/api/public-config.js`가 공개 URL과 공개 키 두 필드만 전달합니다. 비밀 키/service-role 키를 공개 설정에 잘못 넣으면 503으로 차단하고 설정 응답을 캐시하지 않습니다.
- 공개 설정은 `public/supabase-public-config.js`의 공개 URL·publishable 키를 기본으로 사용하며, 정상 동작하는 `/api/public-config.js` 응답이 있으면 배포 환경설정으로 대체합니다. 기본 설정에는 콘텐츠와 서버 비밀 키가 없습니다. 공개 키를 회전하거나 프로젝트를 바꾸면 이 파일도 함께 갱신해야 합니다. `.env.example`에 필요한 변수 이름을 정리했습니다.
- `/api/ready`는 서버 전용 키로 Supabase 작품 테이블을 읽어 실제 연결 성공 여부를 확인합니다. 응답에 DB 행이나 자격증명은 포함하지 않습니다. 이 상태가 `ok`여도 전체 서비스 권한·결제·SQLite 통합이 완료되었다는 의미는 아닙니다.
- 관리자 로그인 결과에 유효한 관리자 역할이 없으면 성공 처리하거나 임의로 SUPER_ADMIN을 부여하지 않습니다. 기존 인증 체계 통합 자체는 여전히 P0 항목입니다.
- 추가 검증: `npm run test:config -- --live`, `npm run test:data`, `npm run build` 통과. 공개 설정의 비밀 키 누출 차단, 잘못된 공개 키 설정 거절, 갱신된 공개/서버 키 실제 조회를 검사했습니다. 이번 추가 수정 후 실제 브라우저 전체 흐름은 재검증하지 않았습니다.

## 코드에서 수정한 내용

### DB 데이터 표시

- 브라우저에 내장된 작품·독자·작가·댓글 시드와 30개 작품 JSON 자동 로드를 제거했습니다. `SAMPLE_*` 변수는 기존 호출부 호환을 위한 이름이며 초기값은 빈 배열입니다.
- DB에 없는 6개 회차 자동 생성, 7~10화 가상 연재예정 목록, 가상 회차 본문을 제거했습니다. 존재하지 않는 작품/회차를 다른 작품의 1화로 대체하지 않습니다.
- DB 조회 0건은 빈 목록으로 반영하고, 작품 조회 실패는 오류로 전달합니다. 삭제 후 예전 작품이 남는 경로를 수정했습니다.
- 추천·인기·신작 설정을 해제해도 임의 작품으로 빈자리를 채우던 로직을 제거했습니다. 웹소설을 웹툰 또는 완결 작품으로 대신 표시하지 않습니다.
- 작품 목록에서 회차 본문을 미리 가져오지 않고, 실제 열람 시 DB에서 불러옵니다. 이는 노출 최소화이며 DB 접근 통제의 대체 수단은 아닙니다.
- 공개 작품 및 공개 회차만 독자 목록에 표시합니다. 미래 예약 회차는 숨깁니다. 예약 시각에 자동으로 발행하는 서버 작업은 별도 구현이 필요합니다.
- 작가 통계가 0건일 때 가짜 그래프를 만드는 로직, 임의 추천수·계좌·정산금, 브라우저 후원 원장 병합을 제거했습니다.
- Golden Best 조회가 비었을 때 임의의 완독률·추천 사유를 생성하던 경로를 제거했습니다.
- 관심작·구독·독서기록은 Supabase 정규화 테이블에 저장하고 성공 응답 후 화면 캐시를 갱신합니다. 다른 계정의 캐시를 합치거나 삭제한 관심작을 복원하지 않습니다.
- 새 독서기록에는 회차 번호가 아닌 DB 회차 ID를 기록합니다. 기존 잘못된 참조는 원격 정비가 필요하며, 작품과 일치하지 않는 이력은 표시하지 않습니다.
- 가짜 75% 이어보기와 기본 1,000포인트를 제거했습니다. 잔액은 DB 프로필에서 읽습니다.
- 공개 폴더의 시드 JSON과 SQL 설치 파일을 `database/`로 이동했습니다.

### 관리자와 독자 화면 연결

- 누락되어 있던 회차 생성/수정/삭제 함수를 연결했습니다. 회차 편집 시 제목·번호·본문/이미지·무료 정책을 함께 저장합니다.
- 무료 여부, 광고 여부, 접근 정책을 함께 갱신해 충돌을 줄였습니다.
- 작품 생성 시 입력된 작가를 DB에서 확인합니다. 모든 작품을 임의로 작가 ID 1에 배정하지 않습니다.
- 작품 설정의 camelCase/snake_case 매핑을 추가했습니다. 수정/삭제 응답은 대상 레코드까지 확인해 0행 변경을 성공으로 처리하지 않습니다.
- 등록·삭제·일괄 변경에서 실패를 성공으로 안내하던 경로를 수정했습니다. 일부 성공 시 성공 건수만 알립니다.
- 작품·회차·작가 변경 구독을 연결해 DB 재조회 및 홈 갱신을 수행합니다. WebSocket 연결은 확인했으나 다른 관리자 브라우저에서 변경한 실제 쓰기 이벤트의 전달은 미검증입니다.
- 존재하지 않는 함수 참조로 초기화가 중단되는 오류, 로그인 세션 함수의 상호 재귀, 설정 초기화 시 독서 화면이 열리는 오류를 수정했습니다.

### 허위 성공 및 운영 위험 완화

- 일반 Supabase Auth 로그인만으로 SUPER_ADMIN을 부여하지 않습니다. 관리자 테이블의 활성 역할 확인이 필요합니다.
- 광고 타이머만으로 회차를 해금하거나, 브라우저 숫자만 차감해 포인트 결제를 성공시키거나, 대기 후 성인인증을 승인하지 않습니다. 실제 제공업체 연동 전에는 준비 중 안내를 표시합니다.
- 운영 환경의 기존 광고/성인인증 모의 API도 차단합니다.
- 후원 RPC 실패 시 로컬에 가짜 후원과 수익 원장을 만들지 않습니다.
- 작가 가입의 로컬 가상 계정 생성 경로를 차단했습니다. 안전한 Auth/프로필 생성 연동이 완료되어야 재개할 수 있습니다.
- PG 핑 테스트, 설정 저장, 정산 승인 실패를 성공으로 표시하지 않습니다. 일부 설정/집계 함수는 아직 미구현이므로 실패로 안내합니다.
- 서버 기동 시 데모 데이터 자동 주입은 기본 비활성화했습니다. 개발에서만 `SEED_DEMO_DATA=true`로 명시해야 합니다.
- 운영 JWT 키는 32자 이상 환경변수 설정이 필수입니다. 개발에서도 공개된 고정 서명키를 사용하지 않습니다.

## 런칭 전 필수 수정 개발사항

| 우선순위 | 문제 / 현재 상태 | 완료 기준 |
|---|---|---|
| P0 | `readers`, `authors`, `admin_users`, `system_config`의 민감한 열이 공개 조회 가능 | 비로그인 개인정보·비밀번호 필드·서버 비밀 설정 접근 거절. 기존 정책과 함수 권한까지 검사 |
| P0 | 유료·성인·비공개 콘텐츠 DB 직접 조회 우회 가능 | 본문을 보호 테이블/서버로 분리하고 로그인·연령·회차 권한을 서버/DB에서 검증. 클라이언트 상태 변조로 열람 불가 |
| P0 | Supabase Auth, 테이블 비밀번호 비교, 자체 JWT, 가짜 토큰이 혼용됨 | Auth 사용자 UUID와 독자/작가/관리자 FK 통일. 비밀번호 브라우저 조회 금지. 재로그인·만료·탈퇴·역할 변경 검증 |
| P0 | Express는 Prisma SQLite, 주 화면은 Supabase PostgreSQL 사용 | 모든 업무 API를 같은 Supabase DB로 통합. 가입·댓글·정산·작품 관리에 다른 DB로의 대체 경로 제거 |
| P0 | 관리자/작가 권한이 UI나 클라이언트 식별자에 의존 | 모든 변경에 DB 또는 서버 RBAC/소유권 검사. 독자의 관리자 변경 및 타 작가 원고 수정 거절 |
| P0 | 광고·성인인증 모의 구현, 포인트/후원/정산의 원자성 미보장 | 실제 제공업체 서명 검증, 재전송·이중 승인 방지, 잔액 차감과 원장/해금 트랜잭션 통합 |
| P0 | 정산 데이터 저장소가 `revenue_events`, `revenue_periods`, `author_earnings`, `earning_ledger` 등으로 분산 | 월별 대상·금액 단위·확정/지급 상태와 단일 원장 확정. 중복 마감·초과 출금·부분 저장 방지 |
| P1 | 일부 CMS 설정·수익 집계·부가 기능에 누락 함수/안내 버튼 존재 | 제공할 메뉴별 저장→새로고침→다른 기기 확인. 미제공 메뉴는 명시적으로 숨김 |
| P1 | 예약 회차는 SCHEDULED 저장만 수행 | 서버 예약 작업, 중복 실행 방지, 발행 시각·시간대 테스트 |
| P1 | HTML 문자열에 DB 문자열 직접 삽입이 남아 있음 | 작품명·필명·댓글·태그·URL을 포함한 저장형 XSS 테스트와 일관된 출력 이스케이프 |
| P1 | 일부 목록/통계 함수는 조회 오류를 빈 목록·0으로 처리 | 정상 0건과 권한/통신 실패를 구분, 재시도와 에러 표시 |
| P1 | 본문/조회 통계, 사용자 이력, 과거 JSON 중복 데이터 정합성 | 기존 잘못된 회차 FK 정비, 사용자 단위 동기화, 집계 조작 방지 |
| P1 | 배포·운영 점검 미완료 | HTTPS, 도메인/CORS, 비밀키 회전, 백업 복구, 오류 모니터링, 속도 제한, 결제 취소·환불, 운영 콘텐츠 검수 |

프론트엔드에서 버튼을 숨기거나 권한을 확인하는 것만으로 RLS 문제는 해결되지 않습니다. Supabase는 Auth 신원과 RLS를 함께 사용하며 서버 비밀키는 RLS를 우회하므로 브라우저에 둘 수 없습니다. [공식 RLS 안내](https://supabase.com/docs/guides/database/postgres/row-level-security), [API 보안 안내](https://supabase.com/docs/guides/api/securing-your-api).

## 검증 및 미검증 범위

- `npm run build`: 통과. 개발 서버가 Prisma DLL을 점유했을 때 실패하여 서버 종료 후 재실행해 통과했습니다.
- `npm run test:data`: 통과. 빈 DB/통신 실패, 가상 콘텐츠 방지, 목록 본문 미조회, 변경 응답 확인, 본문/무료정책 저장, 일반 사용자의 관리자 승격 거절, 후원 실패 시 로컬 원장 미생성, 공개 상태 필터, 삭제된 관심작의 캐시 제거를 검사합니다.
- 수정된 브라우저 JS 구문 검증: 통과.
- 실제 브라우저에서 Supabase 작품 30개 로드 및 Realtime 채널 연결 확인. `/read/1/1` 직접 접근에서 DB 작품 제목과 본문 5개 문단, 활성 화면 `view-reader`를 확인했습니다. 계측 중 발견한 익명 독서 이벤트 호출 및 누락된 조회수 RPC 호출은 제거/제한했습니다. 공개 조회수의 서버 집계 보강은 남아 있습니다.
- 기존 `npm test`는 `prisma/dev.db`의 사용자/작품 등을 삭제하는 테스트이므로 실행하지 않았습니다. 격리된 테스트 DB를 먼저 구성해야 합니다.
- 검증용 브라우저의 무료 회차 열람 중 기존 조회수 기록 코드가 실행되었습니다. 의도한 스키마 변경이나 시드 생성은 없었으나 해당 열람의 조회수는 변경되었을 수 있습니다.
- 관리자 실계정 DB 쓰기, 신규 가입, 외부 결제, 광고 수익, 성인인증, 다른 기기 간 변경 이벤트, 실제 배포는 완료 검증하지 않았습니다.

## 재개에 필요한 설정과 순서

### 데이터 미표시 장애 후속 수정 — 2026-09-21

- 사용자 제보 후 실제 Supabase를 다시 읽어 작품 30개, 회차 180개, 작가 30명, 독자 11명을 확인했습니다. 이번 개발에서 Supabase 초기화나 이 레코드들의 삭제는 수행하지 않았습니다.
- 저장소에 등록된 운영 주소 `https://webnovels-khaki.vercel.app`에서 홈과 공개 설정 API가 모두 HTTP 500 (`FUNCTION_INVOCATION_FAILED`)임을 확인했습니다. 운영 로그 접근은 없어 원격 예외의 정확한 메시지는 확인하지 못했습니다.
- 이전 변경은 운영 JWT 키 누락 시 앱 import 단계에서 예외를 발생시켰으며, 공개 화면도 환경설정 API에 의존했습니다. 로컬 `.env.local`을 사용하는 테스트만으로 이 배포 문제를 발견하지 못했습니다.
- JWT 설정 누락 시 서버 전체를 중단하지 않고 업무 API를 503으로 제한하도록 변경했습니다. 공개 화면은 별도의 공개 연결 설정을 통해 기존 Supabase 데이터를 계속 조회합니다. 설정 API 실패 응답은 이 기본 설정을 삭제하지 않습니다.
- `scripts/verify_public_availability.cjs`로 운영 모드에서 JWT·Supabase 환경설정을 비운 경우를 재현했습니다. 홈 200, 공개 설정 파일 200, 미설정 업무 API 503을 확인했습니다. 빌드 후 `node scripts/verify_public_availability.cjs`로 재검사할 수 있습니다.
- 같은 재현 서버의 실제 브라우저에서 DB 작품 30개·공개 작가 30개 로드와 홈의 작품명·필명 표시를 확인했습니다. 데이터 회귀 검사 및 실제 Supabase 공개/서버 연결 검사도 통과했습니다.

1. `.env.local` 또는 안전한 비밀 저장소에 유효한 Supabase 관리 토큰을 갱신합니다. 서버용 service-role 키는 서버 데이터 접근용이며 관리 API 토큰과 용도가 다릅니다. 키를 채팅이나 프론트엔드에 넣지 않습니다.
2. 현재 DB의 실제 DDL·제약·함수·정책·Realtime publication을 읽어 확정합니다. 저장소에 서로 다른 세대의 SQL이 있으므로 전체 초기화 SQL을 그대로 실행하지 않습니다.
3. 기존 데이터 백업 후 Auth 연결 및 제한적 RLS 마이그레이션, 단일 DB 서버 연동을 적용합니다. [Realtime publication 안내](https://supabase.com/docs/guides/realtime/subscribing-to-database-changes).
4. 필요한 예시 데이터만 중복 없이 보충하고 테스트 수익·결제 기록은 실제 운영 집계에서 분리합니다.
5. 독자 2명·작가 2명·권한이 다른 관리자 계정으로 교차 권한, 실패 복구, 결제 재전송, 별도 브라우저 반영을 검증한 뒤 배포합니다.
