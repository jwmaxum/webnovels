# 10단계 — 전체 API 전환·RLS·Storage 보호와 구 경로 폐쇄

[전체 계획](improve.md) · 이전: [9단계](improve9.md) · 다음: [11단계](improve11.md)

## 목표와 선행 조건

작가·독자·관리자 활성 기능 전체를 하나의 인증/업무 저장 경로로 전환하고, 기존 직접 접근으로 권한을 우회하지 못하게 한다. 이 단계는 **스테이징에서 운영 전환을 리허설**하는 단계다.

## 대상 파일

- `public/supabase-admin.js`, `public/app.js`, `public/js/**`의 DB/RPC/세션 호출.
- `server/secure-api.mjs`, `functions/api/*`, `database/p0/*`, 신규 증분 정책 SQL.
- `src/routes/*`, `src/config/db.ts`, `prisma/schema.prisma`, `api/index.ts`, `vercel.json`, `package.json`.
- `P0_IMPLEMENTATION_PLAN.md`, `CLOUDFLARE_DEPLOYMENT.md`, `docs/launch/migration-runbook.md`.

## 순차 작업

- [ ] 1단계 기능 목록을 기준으로 모든 활성 API 호출을 전수 대조한다. 가입·초안·게시 외에 독서기록·관심작·구독·댓글·설정·통계·관리자 지원까지 포함한다.
- [ ] 미전환 개인 조회/쓰기를 v2 API로 연결한다. 브라우저 Supabase 접근은 명시적으로 허용한 공개 메타데이터만 남긴다.
- [ ] 개인 프로필·초안·본문·버전·운영 설정·원장 원본의 테이블/뷰/RPC 권한을 차단한다. SECURITY DEFINER 함수와 실행 권한도 점검한다.
- [ ] 공개 작품만 읽는 경로와 서버의 무료/비공개/예약/제재/성인/해금 판정을 일치시킨다.
- [ ] Storage 원본 버킷·객체 권한·서명 URL을 검증한다. DB 차단 후에도 기존 공개 파일 URL로 원고가 보이지 않는지 확인한다.
- [ ] 실제 DDL과 비교한 `002_lockdown_after_cutover.sql` 또는 후속 검증된 마이그레이션을 스테이징에 적용한다. 기존 RPC 폐쇄 영향을 전수 확인한다.
- [ ] `P0_API_ENABLED`와 마이그레이션 상태 이중 확인을 유지한다. 새 프론트·DB 보호·서버 활성화의 원자적 전환이 어렵다면 쓰기 중지 창을 사용한다.
- [ ] SQLite/Express 업무 경로의 운영 호출을 끊고 구 토큰·가짜 로그인·직접 DB 인증을 삭제한다.
- [ ] Express/Prisma가 개발 테스트에 필요하면 격리 용도를 명시한다. 완전 제거 시 npm 스크립트·CI·start/build·의존성·문서도 같이 정리한다.
- [ ] 미사용 Vercel 설정·래퍼는 실제 배포 의존이 없는지 확인 후 제거/보관한다. 단순히 오래됐다는 이유로 활성 배포를 끊지 않는다.
- [ ] 초기 미제공 서버 경로는 일관된 미제공 오류를 반환하고 일반 테이블 프록시를 남기지 않는다.
- [ ] 캐시와 기존 세션을 안전하게 갱신하며 기존 링크·계정 재로그인·진행 중 원고 이관을 확인한다.

## 검증과 완료 조건

- [ ] 익명 키로 개인 정보·초안·예약/비공개 본문·관리 설정을 직접 읽을 수 없다.
- [ ] 브라우저 ID/역할 변조, 구 토큰, RPC 직접 호출, 공개 Storage URL 등 우회 경로를 검사했다.
- [ ] 서버 서비스 키로 접근하더라도 endpoint별 소유권·역할 검사가 작동한다.
- [ ] 독자와 작가의 정상 기능은 잠금 이후에도 모두 동작한다. 빠진 API를 광범위 DB 권한 재개방으로 보완하지 않는다.
- [ ] 초기 서비스는 하나의 Supabase 업무 저장소만 갱신한다. SQLite 성공 응답이 운영 저장으로 오인되지 않는다.
- [ ] 백업 기준과 전환 후 작품·회차·본문·계정·독서기록 정합성을 대조했다.
- [ ] 운영 전환 절차·예상 중지 구간·되돌림 한계·필요 설정 이름이 기록되어 있다.

## 복구·주의 사항

실패 시 쓰기를 제한하고 보존된 데이터로 전진 복구한다. 예전 공개 정책이나 브라우저 비밀번호 비교로 복귀하지 않는다. 보안 정책과 프론트의 세대가 다른 조합으로 운영하지 않는다.

## 실행 기록

- 상태: 미착수
- 시작일 / 완료일: — / —
- 변경 파일 / 커밋: —
- 검증 명령 / 결과 / 증거 경로: —
- DB·Storage 적용 환경 / 버전: —
- 외부 의존성·미해결 사항: —
- 다음 단계 진입 판정: 대기(완료 조건 미검증)
