# 7단계 — 미리보기·즉시 게시·예약·공개본 수정

[전체 계획](improve.md) · 이전: [6단계](improve6.md) · 다음: [8단계](improve8.md)

## 목표와 선행 조건

안전하게 저장한 특정 원고 버전을 작가가 직접 공개하고, 독자가 실제로 읽을 수 있게 한다. 예약은 저장 UI뿐 아니라 실행·실패 복구까지 완성한다.

## 대상 파일

- `public/js/creator/creator.js`의 `handleCreateEpisodeSubmit`, 편집기 상태 연결.
- `public/js/reader/reader.js`의 본문 렌더링, `public/index.html`, `public/styles.css`.
- `server/secure-api.mjs`, 게시/예약 트랜잭션, 2단계 공개본·작업 테이블.
- 신규 제안: 독자/미리보기 공용 렌더러와 예약 실행기. Pages 라우트만으로 예약이 실행된다고 가정하지 않는다.

## 순차 작업

- [ ] 실제 리더와 같은 렌더러로 모바일/PC 미리보기를 제공한다. 소유자만 초안을 미리 볼 수 있게 한다.
- [ ] 게시 직전 제목·본문·작품 정보·등급·권리 확인·공개 방식을 요약한다.
- [ ] 기본은 초안 저장이며 게시 버튼은 명확히 구분한다. 검증되지 않은 AI 검수·등급 자동 확인 문구를 제거한다.
- [ ] 무료 초기 범위에 맞게 신규 회차 정책을 서버에서 제한한다. 기존 유료 회차는 임의 무료화하지 않는다.
- [ ] 서버에서 작품 소유권·운영 제재·필수 정보·회차 번호를 검증한다. 배열 길이+1 대신 예약·초안/공개 번호 계약에 맞춰 제안하고 고유 제약으로 보장한다.
- [ ] 게시 요청의 멱등키와 불변 원고 버전을 사용해 원자적으로 공개 상태를 확정한다.
- [ ] 타임아웃은 실패로 단정해 새 회차를 만들지 말고 같은 요청의 확정 결과를 조회한다.
- [ ] 공개본 수정은 별도 편집 초안에서 수행한다. 저장만으로 기존 공개본을 바꾸지 않고 재게시 때 새 버전을 적용한다.
- [ ] 예약 시각·표시 시간대·UTC 저장을 일치시키고 대상 원고 버전을 고정한다.
- [ ] 예약 실행 인프라를 구성하고 인증된 서버만 작업을 실행하게 한다. 중복 실행·동시 실행·재시도·지연 복구를 처리한다.
- [ ] 예약 수정/취소와 실행이 경쟁할 때 조건부 상태 전이로 최종 결과를 확정하고 작가에게 표시한다.
- [ ] 제재된 작품·휴지통 콘텐츠는 예약 시각에도 공개하지 않는다. 실패 사유와 운영 알림을 남긴다.
- [ ] 게시 성공 후 독자 화면 보기·링크 복사·다음 회차 작성을 제공한다. 다음 원고 자동저장을 반드시 초기화한다.
- [ ] 공개 전환·철회에 따른 목록 캐시·검색·독자 접근을 일관되게 갱신한다.

## 검증과 완료 조건

- [ ] 미리보기와 공개 리더에서 줄바꿈·작가의 말·본문 내용이 일치한다.
- [ ] 중복 클릭·응답 유실·같은 요청 재시도에서 하나의 회차만 게시된다.
- [ ] 결번·동일 번호 경쟁·타 작가 ID·제재 작품 요청이 안전하게 처리된다.
- [ ] 브라우저가 꺼져도 예약이 한 번 실행되고 취소한 작업은 공개되지 않는다.
- [ ] 예약 직전 수정·취소·작업 중단 후 재시도에서 대상 버전과 상태가 일치한다.
- [ ] 공개본 수정 중 독자는 이전 공개본을 읽고, 재게시 뒤 새 본문을 읽는다.
- [ ] 발행 직후 다음 회차 작성 → 재접속 시 원고가 복구된다.

## 복구·주의 사항

예약 실행기가 미준비라면 이 단계 완료가 아니다. 제한 출시에서 예약을 제외하려면 전체 계획과 범위를 명시적으로 조정하고 서버·화면을 함께 닫는다. 미완료 기능을 예약 완료로 안내하지 않는다.

## 실행 기록

- 상태: 미착수
- 시작일 / 완료일: — / —
- 변경 파일 / 커밋: —
- 검증 명령 / 결과 / 증거 경로: —
- DB·Storage 적용 환경 / 버전: —
- 외부 의존성·미해결 사항: —
- 다음 단계 진입 판정: 대기(완료 조건 미검증)
