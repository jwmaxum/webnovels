# 11단계 — 품질·보안·모바일·성능 및 운영 검증

[전체 계획](improve.md) · 이전: [10단계](improve10.md) · 다음: [12단계](improve12.md)

## 목표와 선행 조건

스테이징에서 전체 전환을 마친 서비스를 출시 후보로 검증한다. 미구현 기능을 모의 결과로 통과시키지 않고 핵심 검증 실패는 배포를 막는다.

## 대상 파일과 산출물

- `.github/workflows/ci.yml`, `package.json`, `scripts/verify_*.{ts,cjs,mjs}`.
- `public/styles.css`, 화면·API 오류 수정 파일.
- 신규 제안: `docs/launch/acceptance-results.md`, `operations-runbook.md`, `release-checklist.md`.

## 순차 작업

- [x] 검증 스크립트의 DB 접근·삭제 동작을 먼저 읽었다. 과거 `npm test`의 SQLite `deleteMany()`·localhost 테스트는 실행하지 않았다.
- [x] 필수 회귀를 mock API/격리 PGlite로 묶고 CI의 `continue-on-error`를 제거했다. 실제 Storage 인수는 대기다.
- [x] `test:data`, `test:p0`와 가입·초안·파일·게시·예약·독자·관리자 API/RLS 계약을 `npm test`로 묶었다. 과거 삭제 테스트는 회귀 게이트에서 제외했다.
- [ ] Cloudflare 3개 진입점의 esbuild Worker 대상 번들과 mock 라우팅/환경 누락은 검증했다. 실제 Wrangler/Pages 배포는 대기다.
- [ ] 작품/필명/태그/댓글/본문/URL의 저장형 XSS, 파일 업로드, 권한 우회, 요청 중복·경쟁을 시험한다.
- [ ] 실제 PC/모바일에서 한글 조합·긴 원고·키보드 가림·스크롤·포커스·버튼 크기·대비·스크린리더 상태 알림을 확인한다. 실행 권한이 없는 브라우저 검증은 미실행으로 남긴다.
- [ ] 긴 원고 Diff 미리보기를 최대 202행으로 제한하고 2만 줄 입력을 로컬에서 측정했다. 실제 UI·자동저장·여러 파일 변환 응답성은 대기다.
- [ ] 작가/독자/관리자 코드 로딩을 분리하고 페이지 단위 조회·필요 필드만 조회한다. 공용 함수 참조가 깨지지 않게 한다.
- [ ] 저장 실패·예약 지연·API 오류·가입 실패·파일 변환 실패 모니터링과 운영 알림을 연결한다.
- [ ] 비밀값·원고 전문을 로그에 남기지 않는다. 로그 보관·접근·요청 식별자를 정한다.
- [ ] DB+Storage 복원, 예약 중단 후 재처리, 메일 실패, 장애 공지·지원 대응을 훈련한다.
- [ ] 가입/게시 전환율·이어쓰기 소요 시간·파일 실패율 등을 원고 내용 없이 계측한다.
- [ ] 이용/개인정보/콘텐츠 권리/신고·이의제기/탈퇴·보관 정책을 현재 기능과 일치시킨다. 필요한 법적 검토는 별도 완료 근거를 받는다.

## 필수 회귀 묶음

| 묶음 | 반드시 포함할 시나리오 |
|---|---|
| 계정 | 가입·중간 실패 재개·재로그인·재설정·만료·정지·역할 변경 |
| 원고 | 작품 전환·회차 변경·두 기기·응답 역전·오프라인·저장공간 부족·버전 복구 |
| 파일 | TXT/DOCX 품질·다중 파일 일부 실패·중복·타 계정 파일 접근·내보내기 |
| 게시 | 더블클릭·타임아웃·결번·공개본 수정·미리보기·발행 후 다음 원고 저장 |
| 예약 | 닫힌 브라우저·취소 경쟁·지연·중복 실행·실패 재처리·제재 작품 |
| 독자 | 검색·다음 회차·관심작·구독·이어보기·댓글·리더 설정·공개 철회 |
| 관리자 | 통합 처리함·권한 제한·감사·삭제 함수 잔존 호출·구 메뉴 링크 |
| 운영 | RLS/RPC/Storage 우회·백업 복원·비밀 누출·환경 누락·실패 시 배포 차단 |

## 완료 조건

- [ ] 필수 회귀가 실행 환경·명령·결과와 함께 기록되었다. 모의/통합/실제 브라우저 결과가 구분된다.
- [ ] 원고 손실, 권한 우회, 게시/예약 중복, 계정 진입 불가의 미해결 결함이 없다.
- [ ] 필수 검증 중 미실행 항목이 없고, 비핵심 잔여 항목은 영향과 후속 책임 단계가 명시된다.
- [ ] 출시 후보에서 백업 복원·알림·지원 대응이 확인되었다.

## 복구·주의 사항

발견한 결함은 원인 단계에 반영하고 영향 범위만 재검증한다. 출시를 맞추기 위해 실패 테스트를 끄거나 검증되지 않은 상태를 성공으로 바꾸지 않는다.

## 실행 기록

- 상태: 로컬 품질 게이트 구축 중 / 출시 보류
- 시작일 / 완료일: 2026-09-24 / —
- 변경 파일 / 커밋: CI·`package.json`·XSS 출력·원고 Diff·검증 스크립트·출시 문서 / 미커밋
- 검증 명령 / 결과 / 증거 경로: `npm test`, `npm run build`, `node scripts/verify_stage11_xss.test.mjs` 통과. [인수 결과](docs/launch/acceptance-results.md), [검증 JSON](artifacts/step11-quality-verification.json).
- DB·Storage 적용 환경 / 버전: 로컬 격리 PGlite만. 실제 DB/Storage 적용 없음.
- 외부 의존성·미해결 사항: [출시 게이트](docs/launch/release-checklist.md) 참고. 10단계 실환경 전환, 실제 Cloudflare/브라우저/모바일/백업·알림, XSS 전수 검사, 정책 검토 대기.
- 다음 단계 진입 판정: 대기(11단계 완료 조건 미검증)
