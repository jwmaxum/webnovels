구현 계획서를 작성했습니다. 핵심 문제를 요약하면:

🚨 발견된 5가지 핵심 문제
Supabase 미연동 — .env.local에 Key가 있으나, 백엔드가 Prisma+SQLite만 사용. Cloudflare Pages에서는 SQLite가 없으므로 관리자 API 전체가 작동 불능
관리자 로그인이 가짜 — isAdminLoggedIn = true만 설정하고 실제 인증 없음
서브 관리자 CRUD가 모의 — showToast()만 호출, DB 저장 없음
대시보드 KPI 하드코딩 — 실시간 통계 조회 없음
수익배분/정산이 모의 — 폼 submit 시 API 미호출
아키텍처 결정 필요
Cloudflare Pages는 정적 호스팅이므로 Express 백엔드가 작동하지 않습니다. 프론트엔드에서 Supabase JS Client를 직접 호출하는 방식으로 전환해야 합니다.

구현 계획서에 상세 내용과 Open Questions를 기재했습니다. 검토 후 진행 승인을 부탁드립니다.

관리자 페이지 UI 점검 및 Supabase 연동 통합 계획
배경 및 현재 상태 분석
🚨 현재 발견된 핵심 문제점
#	문제	영향
1	Supabase 미연동: .env.local에 Supabase URL/Key가 있으나, 백엔드는 Prisma + SQLite 로컬 파일 DB만 사용. Cloudflare Pages 배포 시 SQLite DB가 없어 API가 전혀 작동 안 됨	관리자 기능 전체 미작동
2	프론트엔드 관리자 로그인이 가짜(모의): handleAdminLoginProcess()가 단순히 isAdminLoggedIn = true 설정만 하고 실제 백엔드 /api/auth/login 호출 없음	보안 없음, 아무나 접근
3	서브 관리자 CRUD가 모의(Mock): 생성/수정/삭제 버튼이 showToast()만 호출하고 실제 API /api/admin/sub-admins 호출 안 함	데이터 영속성 없음
4	대시보드 KPI가 하드코딩: Dashboard 진입 시 API /api/admin/dashboard 미호출	실시간 통계 불가
5	수익배분/정산 폼이 모의: submit 시 API 미호출	정산 기능 미작동
아키텍처 결정 사항
IMPORTANT

핵심 결정: 현재 프로젝트는 Cloudflare Pages (정적 호스팅) 에 배포되므로, Express 백엔드 서버는 작동하지 않습니다. 따라서 프론트엔드에서 Supabase JS Client를 직접 호출하는 방식으로 전환해야 합니다.

Proposed Changes
1. Supabase 클라이언트 프론트엔드 통합
[NEW] public/supabase-admin.js
Supabase JS SDK CDN 로드 (@supabase/supabase-js)
index.html의 .env.local Supabase URL/Key를 사용해 클라이언트 초기화
관리자 인증: Supabase Auth (signInWithPassword) 사용
서브 관리자 CRUD: Supabase DB 직접 쿼리 (.from('users').select/insert/update/delete)
[MODIFY] public/index.html
Supabase JS SDK CDN 추가 (<script> 태그)
supabase-admin.js 스크립트 로드
관리자 CMS 섹션 UI 개선:
대시보드 탭 추가 (KPI 카드: 총 회원수, 작가수, 작품수, 에피소드수, 광고뷰수)
서브 관리자 목록을 동적 렌더링 (하드코딩 → Supabase 실시간 fetch)
정산 목록 동적 렌더링
[MODIFY] public/app.js
handleAdminLoginProcess(): Supabase Auth 로그인 API 호출로 교체
handleCreateSubAdminSubmit(): Supabase DB insert 호출로 교체
서브 관리자 권한 수정/비밀번호 변경/삭제: 실제 Supabase 호출
대시보드 KPI 로드 함수 추가
수익배분 계산/마감 폼: Supabase DB 호출
[MODIFY] public/styles.css
대시보드 KPI 카드 그리드 스타일
서브 관리자 카드 반응형 개선
로딩 스피너 스타일 추가
2. Supabase 데이터베이스 테이블 생성
IMPORTANT

현재 Prisma SQLite 스키마를 Supabase PostgreSQL에 마이그레이션해야 합니다. 하지만 초기 단계에서는 관리자 기능에 필요한 핵심 테이블만 Supabase에 생성합니다.

필요 테이블 (Supabase SQL Editor에서 실행):

users (관리자 계정 저장)
revenue_events (수익 이벤트)
author_settlements (작가 정산)
system_config (PG/PASS 설정)
WARNING

Supabase 무료 플랜의 RLS(Row Level Security) 정책을 설정해야 합니다. 관리자 전용 테이블은 service_role key 또는 적절한 RLS 정책 필요.

3. 관리자 UI/UX 직관성 개선
대시보드 (DASHBOARD) 탭을 기본 첫 화면으로:
5개 KPI 카드 (총 회원수, 작가수, 작품수, 에피소드수, 광고뷰수)
최근 정산 이벤트 테이블
미처리 정산 신청 목록
16개 메뉴 네비게이션 클릭 시 해당 서브탭으로 스무스 전환
서브 관리자 카드: 실시간 Supabase 데이터 반영, 권한 배지 시각화 개선
로딩 인디케이터: API 호출 중 스피너 표시
Open Questions
IMPORTANT

Supabase 프로젝트 상태 확인: .env.local의 Supabase URL(ghwabesnydktumeyejnm.supabase.co)에 실제 테이블이 생성되어 있는지, 아니면 빈 프로젝트인지 확인이 필요합니다. 빈 프로젝트라면 SQL로 테이블을 먼저 생성해야 합니다.

IMPORTANT

Supabase Auth vs Custom Auth: 현재 백엔드는 bcrypt + JWT 커스텀 인증입니다. Supabase Auth를 사용할지, 아니면 Supabase DB에 직접 사용자 테이블을 만들고 프론트엔드에서 비밀번호 검증을 할지 결정이 필요합니다. 권장: Supabase anon key + RLS로 관리자 테이블 접근 제한.

Verification Plan
Manual Verification
Cloudflare Pages 배포 후 #admin 접속 → 로그인 모달 표출 확인
최고 관리자 ID/PW로 로그인 → 대시보드 KPI 로드 확인
서브 관리자 생성 → Supabase DB 반영 확인
서브 관리자 권한 수정/비밀번호 변경/삭제 → 동적 UI 갱신 확인
수익배분 집계/마감 → Supabase DB 반영 확인