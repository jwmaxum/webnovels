# 6단계 — 원고 가져오기·표지 업로드·내보내기

[전체 계획](improve.md) · 이전: [5단계](improve5.md) · 다음: [7단계](improve7.md)

## 목표와 선행 조건

안정화된 초안 저장 위에 파일 입력과 원고 백업을 제공한다. 파일 가져오기가 곧 게시가 되지 않도록 한다.

### 명칭·호환 선행 조건 (필수)

- 착수 전에 [AGENTS.md](AGENTS.md)와 [Author / Creator 계약](docs/launch/author-creator-contract.md)을 읽고 `npm run test:naming`을 실행한다.
- Author는 작가 신원·도메인, Creator는 기존 스튜디오 UI·경로다. `/api/v2/creator/*`, `CreatorDraftEditor`, `creator-*` DOM 및 기존 별칭을 이름 통일을 위해 변경하지 않는다.
- 새 파일 기능은 서버의 `actor.author` 및 5단계 원고 엔진을 사용한다. `creatorId`/`authorId` 캐시·필명·기본 ID로 소유자를 추정하거나 과거 관리자 CRUD로 우회하지 않는다.
- Auth UUID, 작가 프로필 ID, 작품 ID, 원고 UUID를 구분하고 기존 원고/파일 저장 키를 보존한다.

## 대상 파일과 계약

- `public/js/creator/creator-editor.js`, `creator.js`, `public/index.html`, `public/styles.css`.
- 신규 제안: 파일 가져오기 UI·변환 모듈, 업로드/내보내기 API, Storage 정책·변환 테스트 픽스처.
- 구현 시 변환 라이브러리의 지원 형식·런타임 호환·라이선스를 확인한다. 현재 의존성이 있다고 가정하지 않는다.

## 순차 작업

- [x] 단일 TXT와 DOCX를 우선 지원한다. 확장자 외 내용·파일 크기·문자 수·압축 해제 한도를 검사한다.
- [x] UTF-8 및 감지 가능한 다른 TXT 인코딩의 미리보기·선택을 제공한다. 깨진 글자를 조용히 저장하지 않는다.
- [x] DOCX 문단·빈 줄·따옴표·기본 문자 처리를 정의한다. 표·주석·각주·이미지 등 미지원 요소는 누락 경고를 보여준다.
- [x] 외부 리소스 참조·실행 가능한 내용은 가져오지 않는다. 원고를 외부 변환 서비스에 자동 전송하지 않는다.
- [x] 드래그/파일 선택 → 변환 미리보기 → 제목·순서 확인 → 비공개 초안 생성으로 연결한다.
- [x] 여러 파일은 파일명 순서를 제안하고 작가가 조정한다. 파일별 성공/실패, 재시도, 취소를 제공한다.
- [x] 가져오기 작업 ID와 파일 식별자를 사용해 응답 유실·재시도 시 중복 초안을 만들지 않는다. 같은 내용을 의도적으로 다시 가져오는 선택은 구분한다.
- [x] 기존 편집 원고를 대체할 때 새 초안으로 가져오기를 기본으로 하고 덮어쓰기는 백업 후 명시 선택하게 한다.
- [x] 표지 이미지는 선택·미리보기·자르기·용량/픽셀 제한·안전한 재인코딩을 제공한다. 원본과 공개 파생본의 권한을 구분한다.
- [x] 업로드 파일의 작가 소유권·서명 URL 만료·접근 제한을 서버에서 검사한다. 고아 파일 정리는 확정 참조 기준으로 처리한다.
- [x] 회차 TXT와 작품 전체 원고 묶음 내보내기를 제공한다. 작품/회차 제목·순서·공개/초안 구분을 포함하고 유효한 파일명을 만든다.
- [x] HWP/HWPX와 한 파일 자동 회차 분리는 미지원 안내를 제공하고 13단계로 남긴다.

## 검증과 완료 조건

- [x] 한글·이모지·따옴표·연속 빈 줄·혼합 개행 원고가 의도한 내용으로 변환된다.
- [x] 손상된 DOCX, 과대 파일, 잘못된 확장자, 지원하지 않는 요소에 구체적 오류가 표시된다.
- [x] 10개 중 일부 실패·중단·재시도에서도 성공분이 중복되지 않는다.
- [ ] 타 작가의 원본 파일/내보내기 링크는 접근 거절되고 만료된 서명 URL은 유효하지 않다.
- [x] 내보낸 파일을 다시 읽어 제목·회차 순서·원고 내용이 일치한다.
- [x] 표지 교체 실패 시 이전 표지가 유지되고 원고는 영향받지 않는다.

## 복구·주의 사항

업로드·변환 실패 때 입력 원본과 작성 중 원고를 보존한다. 공개 표지로 내보내는 이미지에 원본 파일명·불필요한 메타데이터가 노출되지 않도록 한다.

## 실행 기록

- 상태: **로컬 구현·검증 완료 / 실제 DB·Storage·Images·브라우저 인수 대기**. 체크된 항목은 코드와 로컬 대역/합성 DB 검증 기준이며 운영 완료를 뜻하지 않는다.
- 시작일 / 로컬 완료일: 2026-09-23 / 2026-09-23.
- 변경: 파일 변환·큐·이미지 검사·파일 관리 UI, 기존 편집기/작품 화면 연결, IndexedDB v3, `/api/v2/creator/files` API, `007_creator_files.sql`, 테스트·CI·의존성·기본 비활성 플래그.
- 계약과 제한: [파일 기능 계약](docs/launch/creator-files-contract.md). Author 신원과 Creator UI/기존 경로를 유지했다.
- 검증: `npm run test:files`의 5개 테스트 파일 총 24개 통과. 마지막 UI 보강 후 UI 5개·API 5개와 구문 검사를 재실행했다. 기존 naming/auth/works/drafts/secure-api/Cloudflare 회귀 86개와 데이터 계약·TypeScript 검사도 통과했다.
- 증거: [검증 기록](artifacts/step6-file-verification.json). 브라우저는 실행하지 않았다.
- DB·Storage 적용: 합성 PGlite에서 002·006·007 연계와 007 재실행 확인. 실제 Supabase에는 미적용.
- 의존성: fflate 0.8.2(MIT), Node 테스트용 xmldom 0.9.12(MIT). 표지 서버 재인코딩은 Cloudflare `IMAGES` binding 필요.
- 남은 인수: 실제 타 계정/만료된 서명 URL 거절, private Storage/Images 변환·메타데이터 제거, 브라우저 파일 선택/드래그/크롭/다운로드, IndexedDB 업그레이드·모바일. 서명 URL 60초 발급과 소유권 거절은 로컬 검사만 통과했다.
- 보존: 실패 파일/초안/기존 표지 유지, 확정 참조 없는 ABANDONED 파일만 정리 후보로 조회. 실제 영구 삭제는 보관 정책·백업 검증 전 시행하지 않는다.
- 기능 플래그: `AUTHOR_FILES_ENABLED=false` 유지. 배포·운영 DB 변경 미실행.
- 다음 단계: 7단계 로컬 개발 준비 가능. 2~6단계 실환경 인수를 마치기 전 통합 완료·운영 활성화 판정은 보류한다.
