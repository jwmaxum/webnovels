# 6단계 — 원고 가져오기·표지 업로드·내보내기

[전체 계획](improve.md) · 이전: [5단계](improve5.md) · 다음: [7단계](improve7.md)

## 목표와 선행 조건

안정화된 초안 저장 위에 파일 입력과 원고 백업을 제공한다. 파일 가져오기가 곧 게시가 되지 않도록 한다.

## 대상 파일과 계약

- `public/js/creator/creator-editor.js`, `creator.js`, `public/index.html`, `public/styles.css`.
- 신규 제안: 파일 가져오기 UI·변환 모듈, 업로드/내보내기 API, Storage 정책·변환 테스트 픽스처.
- 구현 시 변환 라이브러리의 지원 형식·런타임 호환·라이선스를 확인한다. 현재 의존성이 있다고 가정하지 않는다.

## 순차 작업

- [ ] 단일 TXT와 DOCX를 우선 지원한다. 확장자 외 내용·파일 크기·문자 수·압축 해제 한도를 검사한다.
- [ ] UTF-8 및 감지 가능한 다른 TXT 인코딩의 미리보기·선택을 제공한다. 깨진 글자를 조용히 저장하지 않는다.
- [ ] DOCX 문단·빈 줄·따옴표·기본 문자 처리를 정의한다. 표·주석·각주·이미지 등 미지원 요소는 누락 경고를 보여준다.
- [ ] 외부 리소스 참조·실행 가능한 내용은 가져오지 않는다. 원고를 외부 변환 서비스에 자동 전송하지 않는다.
- [ ] 드래그/파일 선택 → 변환 미리보기 → 제목·순서 확인 → 비공개 초안 생성으로 연결한다.
- [ ] 여러 파일은 파일명 순서를 제안하고 작가가 조정한다. 파일별 성공/실패, 재시도, 취소를 제공한다.
- [ ] 가져오기 작업 ID와 파일 식별자를 사용해 응답 유실·재시도 시 중복 초안을 만들지 않는다. 같은 내용을 의도적으로 다시 가져오는 선택은 구분한다.
- [ ] 기존 편집 원고를 대체할 때 새 초안으로 가져오기를 기본으로 하고 덮어쓰기는 백업 후 명시 선택하게 한다.
- [ ] 표지 이미지는 선택·미리보기·자르기·용량/픽셀 제한·안전한 재인코딩을 제공한다. 원본과 공개 파생본의 권한을 구분한다.
- [ ] 업로드 파일의 작가 소유권·서명 URL 만료·접근 제한을 서버에서 검사한다. 고아 파일 정리는 확정 참조 기준으로 처리한다.
- [ ] 회차 TXT와 작품 전체 원고 묶음 내보내기를 제공한다. 작품/회차 제목·순서·공개/초안 구분을 포함하고 유효한 파일명을 만든다.
- [ ] HWP/HWPX와 한 파일 자동 회차 분리는 미지원 안내를 제공하고 13단계로 남긴다.

## 검증과 완료 조건

- [ ] 한글·이모지·따옴표·연속 빈 줄·혼합 개행 원고가 의도한 내용으로 변환된다.
- [ ] 손상된 DOCX, 과대 파일, 잘못된 확장자, 지원하지 않는 요소에 구체적 오류가 표시된다.
- [ ] 10개 중 일부 실패·중단·재시도에서도 성공분이 중복되지 않는다.
- [ ] 타 작가의 원본 파일/내보내기 링크는 접근 거절되고 만료된 서명 URL은 유효하지 않다.
- [ ] 내보낸 파일을 다시 읽어 제목·회차 순서·원고 내용이 일치한다.
- [ ] 표지 교체 실패 시 이전 표지가 유지되고 원고는 영향받지 않는다.

## 복구·주의 사항

업로드·변환 실패 때 입력 원본과 작성 중 원고를 보존한다. 공개 표지로 내보내는 이미지에 원본 파일명·불필요한 메타데이터가 노출되지 않도록 한다.

## 실행 기록

- 상태: 미착수
- 시작일 / 완료일: — / —
- 변경 파일 / 커밋: —
- 검증 명령 / 결과 / 증거 경로: —
- DB·Storage 적용 환경 / 버전: —
- 외부 의존성·미해결 사항: —
- 다음 단계 진입 판정: 대기(완료 조건 미검증)
