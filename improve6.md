# [Improvement Step 6] 심층 독자 분석(Deep Reader Analytics) 시각화 및 통합 검증 (improve6.md)

## 1. 개요 및 목적
`reader_events` 테이블과 실시간 열람/완독 추적 파이프라인이 수집하는 원천 데이터를 가공하여, **작가가 "어느 회차에서 독자가 대량 이탈하는지", "어느 문단에서 반응이 폭발했는지", "1화 독자가 3화, 5화까지 얼마나 이어지는지"를 한눈에 파악할 수 있는 시각화 대시보드**를 완성하고, 전체 1~6단계 기능에 대한 **자동화 통합 검증 스크립트(`scripts/verify_author_suite.ts`) 및 빌드 무결성 점검**을 수행합니다.

---

## 2. 변경 대상 파일 목록
1. `public/index.html` : 작가 스튜디오 '독자 통계 (Tab 4)' 내 시각적 독서 퍼널 그래프, 회차별 이탈 히트맵, 일별 이벤트 추이 차트 컨테이너 추가
2. `public/js/creator/creator.js` : 독자 이벤트 집계 및 SVG/Canvas 기반 경량 퍼널 및 바 차트 렌더링 함수
3. `scripts/verify_author_suite.ts` : 초안 이중 저장/충돌, 리더 개인화, 고급 검색, 문단 댓글, 후원 원장, 독자 이벤트 전체를 자동 검증하는 E2E 테스트 스크립트 신설
4. `package.json` : `"test:author"` 실행 스크립트 등록 (선택적)

---

## 3. 세부 기능 개발 명세

### 3.1 작가 스튜디오: 3대 핵심 분석 시각화 차트
1. **연재 독서 퍼널 (Funnel Chart)**:
   - `1화 유입` -> `3화 도달` -> `5화 완독` -> `최신화 정주행` 단계별 독자 잔존율(%)을 수평 퍼널 바 차트로 시각화.
   - 어느 구간에서 독자 이탈이 급증했는지 한눈에 파악하여 작가가 초반 전개 개선 포인트를 발견하도록 지원.
2. **회차별 완독률 & 평균 체류 진행률 비교 (Bar/Progress List)**:
   - 각 회차별 완독률(%)과 스크롤 진행률을 비교 표시.
   - 독자가 100% 끝까지 읽은 비율이 높은 '킬러 회차' 하이라이트.
3. **일자별 독서 활동 타임라인 (Timeline Activity)**:
   - 최근 7일/30일간의 열람(`OPEN`), 진행(`PROGRESS`), 완독(`COMPLETE`) 이벤트 추이.

### 3.2 개인정보 보호 및 표본 신뢰성 기준 (기획서 준수)
- 통계 표시 시 "상관관계 분석이며 인과관계를 확정하지 않습니다" 안내 문구 표기.
- 최소 10건 이상의 유효 이벤트 표본이 쌓였을 때만 백분율을 산출하며, 표본 부족 시 "데이터 수집 중 (N/10건)"으로 안내하여 왜곡 방지.
- 봇이나 비정상 단시간 이탈(1초 미만 탭 닫기) 필터링.

### 3.3 작가 친화 고도화 전체 통합 검증 (`verify_author_suite.ts`)
- 백엔드와 프론트엔드 연동 계약을 검증하는 종합 스크립트 작성 및 실행:
  1. **초안 저장 및 버전 복구 테스트**: revision 증가, 충돌 시 409 반환, 복구 API 동작.
  2. **고급 검색 & 태그 필터 테스트**: 복수 태그 및 완결/회차수 구간 필터링 정확도.
  3. **문단 댓글 및 정책 테스트**: 앵커/인용문 저장, 금칙어 차단, 최소열람회차 미달 차단, 작가 댓글 숨김.
  4. **후원 및 원장 테스트**: 포인트 차감, 멱등키 중복 방지, `earning_ledger` CONFIRMED 상태 기록.
  5. **독자 이벤트 집계 테스트**: `record_reader_event` 저장 및 통계 계산 검증.
  6. **빌드 무결성 점검**: `npm run build` TypeScript 컴파일 및 린트 오류 무결성 확인.

---

## 4. 검증 체크리스트 및 최종 수용 기준
- [ ] 회차가 있는 작품 선택 시 독자 통계 탭에서 1→3→5화 퍼널 그래프가 정상 렌더링되는가?
- [ ] `scripts/verify_author_suite.ts` 실행 시 모든 시나리오가 에러 없이 Pass되는가?
- [ ] `npm run build` 실행 시 TypeScript 컴파일 에러가 0건인가?
- [ ] 모바일/PC 양쪽 브라우저에서 스크립트 콘솔 에러 없이 모든 탭과 모달이 유려하게 동작하는가?
