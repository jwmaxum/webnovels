# 작가 중심 웹소설 서비스 분석 및 개선안

작성일: 2026-09-23  
분석 대상: 현재 저장소의 프론트엔드, 작가 API, Cloudflare API, DB 정의, 배포·런칭 문서

## 1. 결론

현재 서비스는 독자 화면, 작가 스튜디오, 관리자 CMS, 광고·정산·커머스까지 넓게 구축되어 있다. 그러나 **처음 온 작가가 가입하고, 원고를 가져오거나 작성하고, 안전하게 게시하는 핵심 흐름은 아직 완성되지 않았다.**

가장 먼저 투자할 부분은 다음 다섯 가지다.

1. 작가 가입을 정상화하고, 관리자 화면을 거치지 않는 작품 등록을 제공한다.
2. 원고 자동저장·복구·기기 간 충돌 처리를 안정화한다.
3. 직접 쓰기, 붙여넣기, 파일 가져오기를 하나의 편집 화면에서 제공한다.
4. 미리보기 → 공개 범위 확인 → 게시 → 게시 결과 확인을 연결한다.
5. 실제 운영 환경에서 계정·원고 권한과 데이터 저장 경로를 통일한다.

**추천 초기 범위는 무료 웹소설 작성·연재 서비스다.** 기존 광고 수익 모델을 폐기한다는 의미는 아니다. 광고·결제·정산·성인 콘텐츠는 각각의 운영 검증을 마친 뒤 활성화하는 단계적 출시를 제안한다. 이 범위 선택은 제품 제안이며 확정된 사업 정책이 아니다.

## 2. 분석 범위와 판단 기준

- 이번 분석은 저장소의 실제 코드와 문서를 읽은 정적 분석이다. 코드 존재, 동작 의도, 운영 완료를 구분했다.
- 운영 사이트 로그인·쓰기, 실제 DB 권한 조회, 결제, 광고, 예약 작업은 이번에 실행하지 않았다. 브라우저 사용성 테스트도 수행하지 않았다.
- `LAUNCH_READINESS.md`와 `P0_IMPLEMENTATION_PLAN.md`의 2026-09-21 점검 결과는 과거 기록이다. 현재 원격 환경이 동일하다고 단정하지 않는다.
- 아래의 원고 손실·동시성 문제는 코드에서 확인한 위험 경로다. 실제 브라우저에서 재현 완료한 장애 보고는 아니다.
- 이번 변경은 이 문서 작성뿐이다. 서비스 기능, 운영 데이터, 배포 설정은 변경하지 않았다.
- 우선순위: **P0 = 공개 출시 전 필수**, **P1 = 작가 편의성을 위한 첫 개선**, **P2 = 초기 사용 데이터 확인 후 확장**.

## 3. 현재 구축 상태

| 영역 | 확인된 구현 | 판단 |
|---|---|---|
| 독자 서비스 | 작품 탐색·상세·회차 읽기, 테마, 관심작·독서기록·댓글 관련 코드 | 작가가 게시 결과를 확인할 독자 화면 기반은 있다. 운영 권한·정합성 검증은 별개다. |
| 작가 진입 | 로그인·작가가입 화면과 작가 스튜디오 | 작가가입 처리가 준비 중 안내로 종료되어 신규 작가 유입이 막힌다. |
| 작품 등록 | 작품명·필명·장르·태그·설명·표지 입력 | 작가의 첫 작품 버튼이 관리자용 등록 모달을 호출한다. |
| 회차 집필 | 제목, 본문, 작가의 말, 글자 수, 문단 수, 서식 정리 | 기본 집필 도구는 유지할 가치가 있다. 웹소설 본문과 웹툰 URL 입력이 혼재한다. |
| 초안 보존 | IndexedDB 자동저장, 로컬 버전 최대 10개, 비교·복구, 서버 동기화 코드 | 이미 구현된 기능이다. 새로 만들기보다 전환·충돌·실패 처리부터 보강해야 한다. |
| 원고 가져오기 | textarea 직접 입력·붙여넣기 | 검토한 작가 화면에서 TXT/DOCX 파일 선택·변환·일괄 가져오기 흐름은 확인되지 않았다. |
| 회차 게시 | 즉시 발행, 예약 일시 입력, 무료 여부 선택 | 독자 화면 미리보기 없이 등록 가능. 예약 실행 작업은 기존 점검 문서상 미완료다. |
| 작품·연재 관리 | 작품 목록, 회차 작성 진입, 휴재·완결 등 상태, 댓글 정책 | 작품관리·회차등록·연재관리가 별도 탭으로 나뉜다. 작품 중심으로 합칠 여지가 크다. |
| 통계·수익 | 독자 통계, 광고수익, 판매수익, 정산·원장 화면 | 초기 집필 화면에서 비중이 크다. 실제 거래·집계의 운영 완료와 구분해야 한다. |
| 관리자 | 계정·콘텐츠·신고·광고·정산·팬미팅·굿즈 등 | 초기 작성·연재 목표에 비해 운영 범위가 넓다. 핵심 운영 기능만 먼저 노출할 수 있다. |
| 운영 기반 | Cloudflare Pages, Supabase 직접 접근, 별도 Express/Prisma, v2 보안 API | 과도기 구조다. v2 코드가 있어도 기존 화면 전환과 실제 DB 보호가 완료된 것은 아니다. |

### 유지하고 강화할 장점

- 로컬 자동저장과 버전 비교·복구: 집필 서비스에서 가장 중요한 자산이다.
- 글자 수·문단 수·서식 정리: 복잡한 문서 편집기 없이도 웹소설 집필을 도울 수 있다.
- 작품에서 다음 회차 작성으로 진입하는 버튼: 자주 쓰는 작업의 출발점으로 적합하다.
- 독자용 리더: 같은 렌더링을 작가 미리보기에 재사용할 수 있다.
- 휴재·완결, 댓글 차단·신고, 기본 통계: 연재 운영에 필요하므로 제거 대상이 아니다.

## 4. 작가가 가장 쉽게 게시하기 위한 목표 흐름

### 4.1 처음 방문한 작가

`가입·로그인 → 필명 설정 → 작품 제목으로 비공개 작품 만들기 → 원고 작성/가져오기 → 공개용 정보 보완 → 미리보기 → 게시`

- 처음부터 표지, 긴 소개, 정산 계좌, 연재 요일을 모두 요구하지 않는다.
- 작품 제목만으로 비공개 초안을 시작할 수 있게 한다. 필명은 계정에서 가져온다.
- 소개·장르·이용등급·필요한 권리 확인은 최초 공개 직전에 받는다. 등급 판정이 안 된 콘텐츠를 자동 공개하지 않는다.
- 표지가 없으면 작품 제목을 이용한 기본 표지를 제공한다. 다른 작품의 표지를 임의로 대신 쓰지 않는다.
- 로그인 전 체험 집필을 제공한다면 로컬 초안임을 명시하고, 로그인 후 가져오기는 계정 확인을 거쳐 수행한다.

### 4.2 계속 연재하는 작가

`작가 홈 → 이어 쓰기 또는 다음 회차 → 작성 → 미리보기 → 즉시/예약 게시`

- 홈의 가장 큰 버튼은 최근 초안의 **이어 쓰기**다.
- 작품 안에 `회차 / 작품 설정 / 독자 반응`을 배치한다.
- 회차 목록은 `초안 / 예약 / 공개 / 비공개` 상태와 마지막 저장 시각을 보여준다.
- 작품과 다음 회차 번호를 자동으로 채우고, 작가에게 같은 정보를 반복 입력시키지 않는다.
- 게시 후 `독자 화면 보기`, `다음 회차 쓰기`, `링크 복사`를 제공한다.

### 4.3 이미 원고 파일이 있는 작가

`파일 선택·드래그 → 변환 미리보기 → 회차 경계·순서 확인 → 비공개 초안으로 가져오기 → 게시`

- 1차로 UTF-8 TXT와 DOCX를 지원한다. TXT의 다른 인코딩은 감지 결과를 보여주고 선택할 수 있게 한다.
- 한 파일을 한 회차로 가져오는 단순 방식부터 제공한다.
- 여러 파일은 파일명 기준 순서를 제안하되 작가가 번호·제목·순서를 수정하게 한다.
- 한 파일의 여러 회차 분리는 P2로 두고, 자동 분리 결과는 반드시 확인받는다.
- 문단·줄바꿈·대사 따옴표를 보존한다. 서식 정돈은 원본을 백업하고 선택적으로 적용한다.
- 가져오기는 게시가 아니다. 실패한 파일만 다시 시도할 수 있고, 성공한 파일을 중복 생성하지 않아야 한다.
- HWP/HWPX는 실제 작가 수요와 변환 품질을 확인해 확장한다. 미지원 형식에는 DOCX/TXT 변환 또는 붙여넣기 안내를 제공한다.

## 5. 필요한 기능과 완료 기준

| 우선순위 | 필요한 기능 | 구체적 개선 | 완료 기준 |
|---|---|---|---|
| P0 | 정상적인 작가 가입 | Auth 계정과 작가 프로필 연결, 이메일 확인·로그인 복구, 중복 가입 처리 | 신규 계정이 관리자 개입 없이 자기 작품 초안을 만들 수 있다. |
| P0 | 작가 전용 작품 등록 | 관리자 폼 분리, 작성자 서버 자동 지정, 비공개 생성 기본값 | 작가 A가 B의 이름·ID를 보내도 B의 작품을 생성하거나 수정하지 못한다. |
| P0 | 원고 보존 | 로컬 저장 완료와 서버 저장 완료 구분, 저장 실패 재시도, 안정적인 초안 ID | 탭 이동·회차 변경·재접속·발행 후 새 원고 작성에서도 원고가 섞이거나 사라지지 않는다. |
| P0 | 저장 충돌 처리 | 서버 버전 조건부 갱신, 충돌 사본·비교·복구 | 두 기기의 동시 수정 중 하나가 조용히 덮어써지지 않는다. |
| P0 | 안전한 게시 | 서버 검증, 중복 요청 방지, 게시 상태 확정 후 결과 표시 | 더블클릭·응답 유실·재시도에도 회차가 한 번만 생성되고 원고는 보존된다. |
| P0 | 예약 게시의 신뢰성 | 제공하려면 서버 실행기·재시도·취소·시간대 처리까지 구현 | 브라우저를 닫아도 약속한 시간에 한 번만 공개된다. 미완료라면 예약 메뉴를 숨긴다. |
| P1 | 집중 집필 화면 | 넓은 편집 영역, 제목·본문·작가의 말, 저장 상태와 주요 버튼 고정 | 관리 메뉴를 반복 이동하지 않고 한 회차를 끝낼 수 있다. |
| P1 | TXT/DOCX 가져오기 | 파일 검증, 변환 미리보기, 실패 이유, 원본 보존 | 한글·문단·따옴표가 보존되고 지원하지 않는 요소를 안내한다. |
| P1 | 여러 원고 가져오기 | 파일별 상태·순서·중복 확인, 초안 일괄 등록 | 일부 실패 시 성공분은 유지하며 실패분만 재시도한다. |
| P1 | 독자 화면 미리보기 | 실제 리더와 같은 본문 처리, 모바일·PC 폭 전환 | 게시 전 줄바꿈·작가의 말·등급·공개 방식까지 확인한다. |
| P1 | 회차 관리 통합 | 검색·필터·초안 이어쓰기·예약 변경·비공개 전환 | 공개·예약·초안을 같은 작품 화면에서 찾고 수정한다. |
| P1 | 표지 업로드 | 이미지 선택·자르기·미리보기·용량 검증 | 기본 이미지 목록 외에 자기 표지를 올리고 교체할 수 있다. |
| P1 | 원고 내보내기 | 회차 TXT, 작품 전체 원고와 제목·순서 백업 | 계정·브라우저 문제에 대비해 작가가 자기 원고 사본을 가진다. |
| P1 | 모바일·접근성 | 키보드 가림·한글 조합 입력·포커스·대비·버튼 크기 점검 | 실제 휴대폰에서 작성·저장·미리보기·게시를 완료한다. |
| P2 | 연재 일정 관리 | 반복 요일·시간 설정, 예약 목록·실패 알림 | 개별 예약이 안정화된 다음 묶음 일정까지 처리한다. |
| P2 | 작가 반응 요약 | 최근 회차 조회·관심·댓글, 지표 정의·표본 수 | 통계 조회 실패와 실제 0건을 구분한다. |
| P2 | 선택형 집필 보조 | 맞춤법·중복어·문단 제안 | 원문을 자동 덮어쓰지 않고 제안별로 수락·취소한다. |

추천 편집 화면의 기본 버튼은 `초안 저장`, `미리보기`, `게시`다. 버전 관리·서식 정리·가져오기·내보내기는 보조 도구로 묶는다. 기본 저장 상태는 `기기에 저장됨 / 서버에 저장됨 / 재시도 필요 / 충돌 확인 필요`처럼 작가가 이해할 수 있는 말로 표시한다.

## 6. 현재 코드에서 먼저 해결할 집필·게시 문제

### 6.1 신규 작가가 시작할 수 없음 — P0

`public/js/reader/reader.js`의 `handleAuthorSignup()`은 입력 확인 후 가입 준비 중 안내로 끝난다. 가입 화면이 있다는 사실만으로 온보딩이 완료된 것으로 볼 수 없다.

**조치:** 안전한 가입·프로필 연결을 완성하고 가입 직후 첫 작품 생성으로 연결한다. 완료 전에는 가입 완료를 기대하게 하는 일반 공개 모집을 하지 않는다.

### 6.2 작가가 관리자 작품 등록창을 사용함 — P0

`creator.js`의 첫 작품 버튼은 `openAdminCreateWorkModal()`을 부른다. `index.html`의 해당 모달에는 `[관리자] 신규 작품 등록`, 필명 직접 입력, 기존 표지 파일 선택이 있다.

**조치:** 작가용 간단 등록 화면을 분리하고 작성자는 서버 로그인 신원으로 결정한다. 이는 화면 재사용 문제와 별개로 서버 소유권 검증이 필요하다.

### 6.3 작품·회차 전환 시 초안이 섞일 위험 — P0

`creator-editor.js`의 `loadDraft()`는 선택값이 바뀐 후 `saveLocal()`을 호출한다. 그런데 `saveLocal()`은 이전 `currentKey`가 아니라 현재 폼 선택값으로 저장 키를 다시 만든다. 이전 원고가 새 작품·회차 키에 저장될 수 있다. 복원할 초안이 없는 경우 입력값을 명시적으로 비우는 처리도 확인되지 않는다.

또한 `prepareNewEpisodeForWork()`는 선택값을 프로그램으로 바꾸지만 편집기의 초안 전환 함수를 명시적으로 호출하지 않는다.

**조치:** 변경 전 원고와 원래 초안 ID를 먼저 확정 저장한 뒤 새 초안을 로드한다. 진행 중인 자동저장 타이머도 이전 초안에 묶고, 복구할 원고가 없으면 빈 편집 상태로 전환한다.

### 6.4 발행 후 다음 원고의 자동저장이 멈출 위험 — P0

`clearCurrentDraft()`는 `currentKey = null`로 바꾼다. `saveLocal()`은 키가 없으면 즉시 종료한다. 다음 회차 선택값만 프로그램으로 바꾸고 편집기 상태를 새로 초기화하지 않으면 다음 원고의 입력 이벤트가 발생해도 저장되지 않을 수 있다.

**조치:** 게시 성공 후 해당 초안을 게시 완료 상태로 기록하고, 다음 회차 작성 시 새 초안 ID를 즉시 만든다. 로컬 삭제뿐 아니라 서버 초안도 게시 완료로 연결해 오래된 원고가 다시 살아나지 않게 한다.

### 6.5 서버 동기화 경로별 충돌 처리 차이 — P0

Express 경로에는 버전 불일치 시 409 응답을 다루는 코드가 있지만, 편집기는 숫자형 ID와 DB 함수가 있으면 Supabase 직접 저장을 우선한다. `saveEpisodeDraftToDB()`는 기존 버전을 읽고 1을 더해 upsert하며, 전달된 기준 버전과 비교하는 원자적 조건이 없다.

**조치:** 하나의 인증된 저장 API에서 버전 조건부 갱신을 수행한다. 충돌 시 양쪽 원고를 남기고 복구 선택을 제공한다. Express에 충돌 코드가 있다는 이유로 실제 작가 저장 경로도 보호된다고 판단하지 않는다.

### 6.6 저장 완료 표시의 근거가 약함 — P0

IndexedDB 래퍼 `dbRequest()`는 요청의 성공 이벤트에서 완료 처리한다. 저장 트랜잭션의 최종 완료·중단을 기준으로 상태를 확정하도록 보강할 필요가 있다. 서버 응답 대기 중에도 입력이 바뀔 수 있으므로 응답이 어떤 원고 버전을 저장했는지도 추적해야 한다.

**조치:** 수정 여부와 로컬·서버 저장 버전을 별도 상태로 관리한다. 저장공간 부족, 세션 만료, 네트워크 단절 시 경고와 원고 다운로드를 제공한다. 단순히 저장 상태 문구의 CSS 클래스만으로 이탈 안전성을 판단하지 않는다.

### 6.7 실제 검사보다 강한 발행 안내 — P0

화면에는 `AI 자동 검수`, `금칙어/욕설 필터링`, `연령등급 일치 확인`이 표시된다. 확인한 `handleCreateEpisodeSubmit()` 경로는 제목·본문, 최소 5자, 예약 시각 등을 검사하고 DB에 등록한다. 이 경로에서 안내 수준의 AI·등급 검수 호출은 확인되지 않는다.

**조치:** 현재 구현에 맞춰 `입력 항목 확인` 등으로 문구를 수정한다. 콘텐츠 검수가 필요하면 별도의 검수 상태·실패 사유·재신청 절차를 구현한다. 규격 통과와 내용 검수를 같은 성공으로 표시하지 않는다.

### 6.8 회차 번호·접근 정책의 기본값 — P0

다음 회차 번호는 일부 경로에서 `회차 배열 길이 + 1`로 계산한다. 삭제·결번이 있으면 기존 번호와 충돌할 수 있다. 무료 체크박스는 기본 선택되지 않고, 미선택 시 광고 정책으로 저장된다. 광고 해금이 미완성인 상태에서는 게시해도 독자가 읽지 못할 수 있다.

**조치:** 서버가 작품별 기존 번호와 초안·예약 상태를 확인하고 중복을 거절한다. 무료로 먼저 출시한다면 기본 정책을 무료로 고정하고 미연동 광고·포인트 선택지를 숨긴다. 정책 변경은 이미 게시된 회차와 구분해 적용한다.

## 7. 필요 없는 부분: 제거·통합·숨김·후순위 구분

여기서 불필요하다는 뜻은 **초기 작가 작성·게시 목표에 불필요하거나, 현재 표현·배치가 불필요하다**는 의미다. 운영 데이터나 사업 기능을 일괄 삭제하자는 제안이 아니다.

| 현재 항목 | 권고 | 이유 및 처리 |
|---|---|---|
| 작품관리·회차등록·연재관리 3개 탭 | 통합 | `내 작품 → 작품별 회차 목록 → 작성`으로 연결하고 휴재·완결은 작품 설정에 둔다. |
| 광고수익·판매수익·정산관리 3개 탭 | 통합·조건부 노출 | 운영 가능해진 뒤 `수익·정산` 하나로 묶는다. 집필 첫 화면의 주 메뉴 비중을 줄인다. |
| 첫 화면 정산 계좌·인증 작가 강조 | 축소·이동 | 집필 시작보다 우선할 이유가 약하다. 계좌는 정산 설정, 배지는 검증된 상태에만 표시한다. |
| 작가용 관리자 등록 모달 | 대체 | 작성자 수동 입력과 관리자 표현을 없애고 작가 전용 등록을 제공한다. |
| 웹소설 본문과 웹툰 URL 쉼표 입력 | 분리·후순위 | 웹소설 편집에서 제거한다. 웹툰을 제공할 때 이미지 업로드·순서 편집을 별도로 만든다. 기존 콘텐츠는 보존한다. |
| 팬미팅·굿즈·티케팅 | 초기 메뉴 숨김 | 작품 게시와 직접 관련이 적고 재고·배송·취소 등 운영 부담이 추가된다. |
| 복잡한 이벤트·고급 추천·다수 통계 | 후순위 | 기본 검색·최근 연재·관심작은 유지하고, 충분한 데이터가 필요한 기능은 나중에 검증한다. |
| 미연동 광고·포인트·후원·성인 인증 메뉴 | 조건부 숨김 | 준비 중 버튼을 반복 경험하게 하지 않는다. 실제 서버 검증 완료 후 활성화한다. |
| AI 자동 검수 보장·Zero-Touch 표현 | 수정 | 구현보다 과장된 약속을 없애고 `작성`, `예약`, `게시`처럼 결과가 분명한 말을 쓴다. |
| `실제 DB 연동`, `Supabase 동기화` 문구 | 수정 | 작가에게는 `서버에 저장됨`, `내 작품`이 더 유용하다. 내부 구조 설명을 걷어낸다. |
| 고정 5화 예시·6화 통계·1개 작품 초기값 | 제거 | 로딩·빈 상태와 실제 데이터만 표시한다. 이용자가 예시를 자기 데이터로 오해하지 않게 한다. |
| 조회수 기반 가상 수익 fallback | 제거 | `creator.js`에 수익 함수 부재 시 조회수 × 22.5 등으로 계산하는 경로가 남아 있다. 실제 매출처럼 표시하면 안 된다. 실행 여부와 무관하게 대체값은 미확인 상태로 바꾼다. |
| 가입·승인 등 안내만 하는 버튼 | 숨김 또는 구현 | 실제 저장·처리 없이 토스트만 띄우는 버튼은 제공 기능처럼 노출하지 않는다. |
| 중복 API·인증·DB 경로 | 단계적 폐기 | 전환·검증 후 제거한다. 현재 SQLite와 Supabase 코드가 공존하므로 파일부터 삭제하면 안 된다. |

**유지할 운영 기능:** 계정 복구, 신고·차단, 원고 비공개 전환, 콘텐츠 검수, 권한 관리, 감사 기록, 백업 복원. 작가 화면에서 숨길 수는 있어도 실제 서비스 운영에서 빼서는 안 된다.

## 8. 실제 서비스를 위한 개선

### 8.1 인증·권한·원고 보호 — P0

- 브라우저에서 계정 테이블의 `password_hash`를 읽어 비교하는 기존 로그인 경로를 Auth 기반으로 전환한다.
- 클라이언트의 작가 ID·필명·역할은 권한 증거로 쓰지 않는다. 매 요청 서버에서 계정·작가 소유권을 확인한다.
- 초안, 비공개·예약 회차, 보호 본문과 공개 메타데이터의 조회 범위를 분리한다.
- RLS뿐 아니라 뷰·RPC·Storage 공개 URL까지 확인한다. 개인 원고와 업로드 원본을 공개 버킷에 두지 않는다.
- 과거 점검에서 보고된 공개 개인정보·보호 본문 노출은 실제 운영 환경에서 재검증하고 차단 완료 근거를 남긴다.
- 공용 기기의 로그아웃·계정 전환 시 다른 작가의 로컬 초안이 표시되지 않게 한다. 미동기화 초안은 경고·내보내기 없이 임의 삭제하지 않는다.

완료 기준: 익명·독자·작가 A·작가 B·제한 관리자·최고 관리자 계정으로 직접 API/DB 요청을 시험하고, 허용하지 않은 원고 조회·변경이 모두 거절된다.

### 8.2 운영 저장 경로 통일 — P0

현재 Cloudflare에서는 Pages Functions가 동작하고, 별도 Express 코드는 Prisma SQLite를 사용한다. 프론트에는 Supabase 직접 쓰기와 Express 요청이 함께 남아 있다. Cloudflare의 구 API catch-all은 미구현 경로에 503을 반환한다.

목표는 **Cloudflare API → 동일 Supabase 프로젝트**로 업무 경로를 통일하는 것이다. 공개 메타데이터의 직접 읽기를 유지할지는 접근 정책에 따라 정하되, 인증·초안·게시·개인정보 쓰기는 일관된 서버 검증을 거쳐야 한다.

`server/secure-api.mjs`는 현재 기능 일부와 활성화 보호 장치를 제공한다. `P0_API_ENABLED`만 켜지 말고 가입·작품 생성·초안·게시·작가 관리 조회까지 옮긴 뒤 기존 경로를 닫아야 한다. 기존 P0 실행 계획의 백업 → 확장 → 계정 연결 → 새 경로 검증 → 전환 순서를 따른다.

### 8.3 초안·공개본·예약의 데이터 모델 — P0

- 초안은 변경 가능한 회차 번호 대신 안정적인 ID로 식별한다.
- 편집 중인 초안과 독자에게 보이는 공개본을 구분한다. 공개 회차 수정 중에는 기존 공개본을 유지한다.
- 게시·예약은 특정 원고 버전을 대상으로 한다. 예약 후 수정 시 예약 대상 버전 변경 여부를 명확히 보여준다.
- 예약 실행에는 중복 방지, 지연 재처리, 실패 기록, 취소·시각 변경을 포함한다. 시간은 UTC로 저장하고 작가에게는 명시한 시간대로 표시한다.
- 작품별 회차 번호 고유 제약과 요청 재시도 식별자를 사용한다.
- 삭제는 우선 휴지통·복구로 제공하고, 영구 삭제 범위와 보관 정책을 별도로 정한다.

### 8.4 업로드·표시 안전성과 성능 — P0/P1

- 파일 크기·형식·실제 내용·변환 결과를 검증한다. DOCX 내 외부 참조나 실행 가능한 내용은 가져오지 않는다.
- 작품명·필명·본문·댓글·URL을 HTML에 넣는 경로의 출력 처리를 통일한다. 현재 작품 카드 등에는 문자열을 HTML에 직접 삽입하는 부분이 있다.
- 작가 목록은 서버에서 본인 작품만 조회하고 페이지 단위로 가져온다. 현재 전체 작품 배열을 필명·제목·ID로 필터링하는 처리는 소유권 검증을 대신하지 못한다.
- 독자·작가·관리자 화면의 공통 HTML과 스크립트 로딩을 나누어 불필요한 초기 처리와 전역 함수 충돌을 줄인다.
- 재작성보다 작가 경로부터 분리한다. 프레임워크 교체 자체를 첫 목표로 삼지 않는다.

### 8.5 배포·장애 대응 — P0

- 미리보기/테스트 DB와 운영 DB를 분리하고 실제 원고로 파괴적 테스트를 하지 않는다.
- DB·원고 파일 백업을 만들고 복원 시험을 수행한다. 백업 파일 존재만으로 복구 가능하다고 판정하지 않는다.
- 저장 실패율, 게시 실패, API 오류, 예약 지연을 모니터링한다. 로그에는 원고 전문·비밀번호·비밀키를 넣지 않는다.
- 배포 전 Cloudflare Functions 번들·환경설정·라우팅·보안 계약을 확인한다. TypeScript 빌드 성공만으로 Pages 런타임을 검증한 것으로 보지 않는다.
- 현재 CI의 백엔드 통합 테스트에는 `continue-on-error: true`가 있다. 출시 필수 검증은 실패하면 배포를 막도록 바꾸고 Cloudflare 실제 경로를 검증한다.
- 배포 후 가입 → 초안 저장 → 게시 → 다른 독자 계정 열람을 테스트 계정으로 확인한다.

### 8.6 수익화·운영 정책 — 해당 기능 공개 전 필수

- 광고 완료는 실제 제공업체 서버 검증 이후에만 해금·수익 이벤트로 인정한다.
- 결제·포인트·후원·정산은 주문·사용자·금액 검증, 중복 처리 방지, 원자적 원장 반영을 갖춘다.
- 예상·확정·지급 가능 금액의 기준을 정의하고 테스트 데이터와 실제 거래를 분리한다.
- 성인 콘텐츠는 검증된 연령 확인과 본문 접근 통제가 준비된 범위에서만 제공한다.
- 콘텐츠 권리, AI 사용 표기, 신고·이의제기, 탈퇴·삭제·보관, 정산 조건을 운영 정책으로 정하고 화면에 반영한다. 구체적인 법적 요건의 적합성 검토는 이번 코드 분석 범위에 포함하지 않는다.

무료 연재부터 시작하면 수익화 구현은 뒤로 미룰 수 있다. 계정 보안·원고 보호·복구·신고 대응은 무료 서비스라도 출시 전에 필요하다.

## 9. 권장 메뉴 구조

| 영역 | 기본 메뉴 | 핵심 내용 |
|---|---|---|
| 작가 | 홈 | 최근 초안 이어쓰기, 다음 회차, 저장·예약 문제 알림 |
| 작가 | 내 작품 | 작품별 회차·초안·예약, 작품 설정, 독자 반응 |
| 작가 | 수익·정산 | 해당 기능이 실제 운영 가능한 작가에게만 노출 |
| 작가 | 설정·도움말 | 필명, 계정, 원고 내보내기, 지원 문의 |
| 관리자 | 계정·권한 | 계정 복구, 작가 상태, 운영자 권한 |
| 관리자 | 작품·신고 | 콘텐츠 검수, 신고, 비공개 조치, 변경 기록 |
| 관리자 | 서비스 운영 | 저장·예약 장애, 공지, 문의, 감사 기록 |
| 관리자 | 수익 운영 | 수익화 활성화 시 광고·거래·정산 관리 |

모바일에서는 `내 작품`, `이어 쓰기`, `더보기`처럼 자주 쓰는 진입점을 우선한다. 7개 탭을 작은 화면에서 그대로 나열하지 않는다.

## 10. 실행 순서

| 단계 | 작업 묶음 | 다음 단계로 넘어가는 조건 |
|---|---|---|
| 1. 운영 기반·원고 보호 | 계정/Auth 연결, 실제 권한 점검, 단일 API 전환, 초안 식별·충돌·저장 상태 수정 | 타인 원고 접근 차단, 원고 전환·복구·재시도 검증 통과 |
| 2. 첫 게시 완성 | 작가 전용 작품 등록, 집중 편집기, 미리보기, 무료 게시, 게시 결과 확인 | 신규 작가가 가입부터 첫 회차 독자 열람까지 도움 없이 완료 |
| 3. 원고 가져오기·연재 편의 | TXT/DOCX, 표지 업로드, 일괄 초안, 내보내기, 예약 실행·관리 | 파일 변환·일부 실패 복구·예약 취소·중복 실행 검증 통과 |
| 4. 제한 베타 | 소수 실제 작가 사용 관찰, 문의 대응, 모바일 검증, 백업 복원 | 게시 과정의 막힘과 원고 손실 미해결 문제가 없고 장애 대응 가능 |
| 5. 수익화·확장 | 광고·정산·성인 콘텐츠·고급 통계·웹툰 등 | 기능별 실제 업체·거래·권한 검증 후 개별 활성화 |

필요 인력과 기존 DB 적용 상태가 확인되지 않아 일정 일수는 산정하지 않았다. 단계 1에서 운영 구조를 확정한 뒤 작업량을 추정하는 편이 정확하다.

## 11. 출시 전 인수 시나리오

아래는 앞으로 실행할 검증 항목이며 이번에 통과한 테스트 목록이 아니다.

1. 신규 작가가 가입하고 작품 초안을 만든 뒤 첫 무료 회차를 게시한다. 다른 독자 계정에서 같은 본문을 읽는다.
2. 원고를 입력한 직후 작품·회차를 바꿔도 양쪽 원고가 각자의 초안으로 보존된다.
3. 발행을 마친 뒤 바로 다음 회차를 쓰고 새로고침해도 복구된다.
4. 네트워크를 끊고 작성한 뒤 다시 연결하면 저장되며, 실패한 상태를 성공으로 표시하지 않는다.
5. PC와 휴대폰에서 같은 초안을 수정하면 충돌을 알리고 두 원고를 모두 복구할 수 있다.
6. 저장공간 부족·세션 만료·API 오류에도 원고를 유지하고 재로그인·재시도·다운로드를 제공한다.
7. 여러 파일 중 하나가 실패해도 성공분은 중복되지 않고 실패분만 다시 가져온다.
8. 회차 번호에 결번이 있어도 새 회차가 기존 회차를 덮어쓰지 않는다.
9. 게시 더블클릭·응답 유실·재시도에도 공개 회차는 하나이며, 공개 여부를 다시 확인할 수 있다.
10. 예약 시각에 브라우저가 꺼져 있어도 정확히 한 번 공개되고, 취소한 예약은 공개되지 않는다.
11. 공개 회차 편집 중 독자에게는 이전 공개본이 유지된다. 미리보기는 비공개로 제공된다.
12. 작가 A가 B의 원고·초안·표지 원본·개인정보를 읽거나 변경하지 못한다.
13. 공용 기기에서 로그아웃하고 다른 계정으로 들어가도 이전 계정의 초안이 노출되지 않는다.
14. 백업으로 작품·회차·초안과 파일을 복원하고 원고 내용의 일치를 확인한다.

### 효과 측정 제안

현재 측정값은 없다. 다음 지표를 계측한 뒤 개선 전후를 비교한다.

- 가입 후 첫 초안 저장률, 첫 게시 완료율, 단계별 이탈률.
- 원고가 준비된 작가의 첫 게시 소요 시간. 초기 사용성 목표 예시는 5분 이내이며 검증된 성과가 아니다.
- 재방문 후 이어쓰기까지 걸린 시간과 조작 수.
- 서버 저장 성공률, 충돌 발생·복구율, 원고 손실 신고 건수.
- 파일 가져오기 성공률과 문단·인코딩 오류 비율.
- 예약 게시 성공률·지연 시간, 게시 후 수정·문의 발생 원인.

원고 본문 자체를 분석 로그에 수집하지 않고 작업 이벤트와 처리 결과로 계측한다.

## 12. 근거 파일

| 파일·함수 | 이번 분석에서 확인한 근거 |
|---|---|
| [public/index.html](public/index.html) | 작가 7개 탭, 집필 폼, 무료 기본값, AI 검수 안내, 관리자 작품 등록 모달, 표지 선택, 부가 메뉴 |
| [public/js/creator/creator.js](public/js/creator/creator.js) | `fetchCreatorDashboardData`, `prepareNewEpisodeForWork`, `handleCreateEpisodeSubmit`, 관리자 모달 진입, 회차 번호, 잔존 가상 수익 fallback |
| [public/js/creator/creator-editor.js](public/js/creator/creator-editor.js) | `loadDraft`, `saveLocal`, `syncServer`, `dbRequest`, `clearCurrentDraft`, 로컬 버전·서식 도구 |
| [public/js/reader/reader.js](public/js/reader/reader.js) | `handleAuthorSignup`의 가입 미처리 안내 |
| [public/supabase-admin.js](public/supabase-admin.js) | 브라우저 계정 비교, 전체 목록 조회, `createEpisodeInDB`, `saveEpisodeDraftToDB` |
| [src/routes/creator.router.ts](src/routes/creator.router.ts) | 별도 Express 초안·버전·작품·회차 API |
| [prisma/schema.prisma](prisma/schema.prisma) | SQLite 데이터소스 정의 |
| [server/secure-api.mjs](server/secure-api.mjs) | v2 활성화·마이그레이션 확인, 서버 신원·소유권 검사, 현재 제공 경로 |
| [functions/api/[[path]].js](functions/api/%5B%5Bpath%5D%5D.js) | Cloudflare 구 API의 미구현 응답 |
| [.github/workflows/ci.yml](.github/workflows/ci.yml) | 기존 검증 구성, 일부 통합 테스트 실패 허용 |
| [LAUNCH_READINESS.md](LAUNCH_READINESS.md) | 2026-09-21 과거 런칭 점검 및 원격 미검증·미완료 항목 |
| [P0_IMPLEMENTATION_PLAN.md](P0_IMPLEMENTATION_PLAN.md) | 보안 전환 단계·미완료 작업·기존 데이터 보존 원칙 |
| [CLOUDFLARE_DEPLOYMENT.md](CLOUDFLARE_DEPLOYMENT.md) | 운영 배포 기준, API·DB 전환 절차와 제한 |

기존 개선 문서의 모든 내용을 합치기보다, **작가의 첫 게시와 지속 집필을 막는 요소를 기준으로 재우선순위화**했다. 구현에 착수할 때는 이 문서의 사용자 흐름과 기존 P0 실행 계획의 보안 전환 절차를 함께 적용한다.
