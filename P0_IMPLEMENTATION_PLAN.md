# P0 개선 실행 계획 — Cloudflare / Supabase

## 전제와 보존 원칙

- 운영 배포는 **Cloudflare**이다. 저장소의 Vercel 주소에서 수행한 점검은 Cloudflare 운영 검증으로 인정하지 않는다.
- 작품 30개·회차 180개·작가 30명·독자 11명의 기존 레코드와 식별자를 유지한다. 초기화, 전체 재시드, 암호의 임의 변경을 하지 않는다.
- 기존 비밀번호를 Auth로 임의 복사하거나 이메일 문자열만으로 계정을 합치지 않는다. 검증된 계정 매핑과 비밀번호 재설정 절차를 사용한다.
- 코드 작성, 모의 테스트, 실제 DB 적용, Cloudflare 배포, 운영 교차 권한 검증은 별도 상태로 기록한다.
- 변경은 확장 → 계정 연결 → 새 API 검증 → 프론트 전환과 공개 권한 차단 → 구 경로 폐쇄 순서다. 데이터 복원 없이 보안 정책을 되돌려 개인정보를 재공개하는 롤백은 허용하지 않는다.

## 순차 개발 및 통과 조건

| 단계 | 구현 범위 | 선행 조건 / 완료 기준 | 상태 |
|---|---|---|---|
| 0 | 실제 DDL·RLS·뷰·함수·권한·Storage·Realtime 목록, 데이터 수·연결관계 기준선, 백업·복구 절차 | DB 관리 접근, 백업 식별자, 실제 Cloudflare 주소/프로젝트 | 공개 데이터 기준선 확인. 관리 API 401로 정책 덤프·백업 확인 대기 |
| 1 | Cloudflare 표준 Request/Response API, Supabase 단일 저장소, 서버 비밀 키 격리, Auth 토큰 검증 | 가짜 토큰·만료 토큰·설정 누락 거절; SQLite 의존 없음 | 코드·모의 검사·Pages 번들 빌드 완료. 보안 경로 활성화 대기 |
| 2 | Auth UUID 연결, 관리자 역할/세부 권한·작가 소유권, 공개/개인 프로필 분리 | 독자→관리자 거절, 작가 A→B 원고 거절, 중지/역할 변경 즉시 반영 | 서버 검사 구현. 실제 계정 매핑·교차 계정 검증 미완료 |
| 3 | 본문 보호, 원본 테이블·뷰·RPC 권한 차단, 무료/유료/성인/비공개 서버 판정 | 익명 직접 조회 차단, 권한 없는 본문 조회 차단, 무료 공개 작품 보존 | 서버 판정과 SQL 초안 준비. SQL 미실행, 실제 RLS 미변경 |
| 4 | 독서기록·관심작·구독·댓글·원고·관리자 설정·통계의 Supabase API 전환 | 모든 변경에서 서버 신원/소유권 확인; SQLite 대체와 클라이언트 비밀번호 비교·가짜 토큰 제거 | 2~3 통과 후 전환 |
| 5 | 결제·광고·성인인증 업체 서버 검증, webhook 재전송 방지, 포인트/해금 원자적 처리 | 업체·상점/앱 설정 확보, 위조/중복/금액 불일치 거절, 트랜잭션 동시성 검사 | 업체 선택·설정 대기; 미연동 기능은 503 |
| 6 | 단일 원장, 월 마감·정산 예약·지급·취소 | 단위/수수료/배분/반올림 확정, 이중 마감·초과 인출·중복 지급 거절 | 5단계의 검증된 이벤트부터 집계 |
| 7 | 기존 화면 새 API 전환, 구 RPC/API 폐쇄, Cloudflare 배포 및 운영 회귀검사 | 독자 2/작가 2/서로 다른 관리자 역할, 재로그인·만료·탈퇴·무료 열람·다른 브라우저 반영 | 모든 P0 완료 후 공개 런칭 판단 |

## 구현 계약

- `/api/v2/*`는 Cloudflare에서 같은 Supabase 프로젝트만 접근한다. 서버가 `/auth/v1/user`에 토큰을 확인한 뒤 DB의 `auth_user_id`로 역할과 상태를 매 요청 재조회한다. localStorage 역할, 요청의 사용자 ID, JWT payload 자체를 신뢰하지 않는다.
- 서버 키로 일반 테이블 프록시를 만들지 않는다. endpoint별 필드 허용 목록·RBAC·소유권 검사를 적용한다. 권한 오류 응답에는 DB 내용이나 키를 넣지 않는다.
- RLS 적용 후 브라우저 직접 접근은 공개 작품 메타데이터·공개 작가 프로필 등 허용한 읽기만 가능하다. 본문과 개인/운영 데이터는 서버 API만 접근한다.
- 새 보안 경로는 DB 마이그레이션 버전과 Cloudflare 설정이 준비된 뒤 전환한다. 코드가 존재한다는 이유로 이전 공개 정책이 해결되었다고 보고하지 않는다.
- ID·잔액·작성자·관리자 역할·성인 여부·지급 상태를 클라이언트 입력으로 갱신하지 않는다.

## 계정·원장 이관

1. 기존 계정 ID와 Auth UUID의 검증된 매핑표를 서버에서 적용한다. 중복 이메일/기존 UUID 충돌은 자동 병합하지 않는다. 미연결 계정은 보존하고 전환 전 로그인 복구 절차를 마련한다.
2. 기존 `password_hash`는 먼저 비공개로 만들고, 로그인 이관 검증 이후 제거 여부를 결정한다. 브라우저로 내보내지 않는다.
3. 기존 수익/정산 테이블은 삭제하지 않는다. 각 행의 출처·외부 거래 ID·확정 여부를 대조해 보관한다. 테스트 수익을 실제 매출이나 출금 가능액으로 합산하지 않는다.
4. 원장은 화폐와 포인트 단위를 분리한 정수 금액, 고유 외부 이벤트 ID, 상쇄 분개 방식으로 설계한다. 출금 예약도 트랜잭션에서 잔액을 잠그고 차감해야 한다. 미확정 전환 환율·수수료율을 임의로 구현하지 않는다.

## 적용 전 필요한 운영 정보

- 확인 완료: `https://webnovels-db4.pages.dev/`, Cloudflare Pages, `npm run build`, 출력 `public`, 자동 배포 `main`. 공개 URL·공개 키·서버 secret 키가 등록되어 있다.
- 유효한 `SUPABASE_ACCESS_TOKEN` 또는 안전한 직접 DB 관리 접속, 백업 확인. service-role 키는 데이터 조회용이며 관리 API 토큰을 대체하지 않는다.
- 기존 Auth 계정 연결 및 관리자 초기 연결 기준.
- 결제/광고/성인인증 업체·운영/테스트 계정, 지급/정산 정책. 비밀값은 채팅·Git에 기록하지 않는다.

## 현재 구현 내역과 남은 작업

- `server/secure-api.mjs`: 신원/역할 확인, 안전한 관리자 조회, 작품·회차 수정, 보호 본문 권한 판정. Cloudflare API는 SQLite를 사용하지 않는다. 이 사실이 기존 모든 업무 API의 전환 완료를 뜻하지는 않는다.
- `functions/api/*`: Pages 런타임 공개 설정과 v2 라우팅. 기존 Cloudflare의 `/api/public-config.js`가 HTML 200을 반환하던 문제를 수정한다. 미구현 API는 JSON 503을 반환한다.
- 기존 Express 결제 경로는 주문·소유자·원장 검증이 없는 상태이므로 차단한다. 단순히 예제 키로 DONE을 반환하던 코드를 실제 결제 완료로 취급하지 않는다.
- 토스 서버 통신 모듈: 서버에 예약된 주문/사용자/금액 확인, 고정 멱등키, 실제 HTTP 호출, 승인 응답 대조, 타임아웃 결과 미확정 처리. 아직 API·주문 트랜잭션에 연결하지 않았다.
- 본인확인 서버 통신 모듈: PortOne V2 완료 기록 재조회, 사용자에 연결된 인증 시도·만료·채널·환경·검증 생년월일 확인. 기존 성인 플래그는 새 본문 API의 증거로 사용하지 않는다. 계약/키/인증 시도 저장/최종 연령 정책/DB 반영은 미완료다.
- 카카오 지원을 위해 KG이니시스 통합인증을 제안한다. 시장 점유율 순위는 확인하지 않았고 계약은 체결하지 않았다. 운영 가이드는 `CLOUDFLARE_DEPLOYMENT.md`에 있다.
- 32개 모의 검사(보안 21, Pages 4, 업체 통신 7)를 작성했다. 실제 DB 정책·결제·본인인증 테스트의 대체가 아니다.
- 단계 4~7과 기존 화면의 새 Auth 세션 전환은 미완료다. 기존 프론트의 직접 DB 인증/쓰기 경로도 남아 있으므로 **P0 해결 완료 또는 런칭 가능으로 판정하지 않는다.**
- DB 마이그레이션 버전과 `P0_API_ENABLED` 이중 확인 전에는 v2가 503으로 닫힌다. 실제 DB 보호 전 공개 서버 키 프록시가 열리지 않도록 한다.
- 신규 DB INSERT/UPDATE/DELETE, DDL/RLS 적용, Auth 계정 생성·삭제는 이번 단계에서 실행하지 않았다.

## 참고 근거

- [Cloudflare Pages Functions 라우팅](https://developers.cloudflare.com/pages/functions/routing/), [비밀값 바인딩](https://developers.cloudflare.com/pages/functions/bindings/)
- [Supabase API 보안](https://supabase.com/docs/guides/api/securing-your-api), [RLS와 함수 권한](https://supabase.com/docs/guides/database/postgres/row-level-security), [Auth 사용자 검증](https://supabase.com/docs/reference/javascript/auth-getuser)
