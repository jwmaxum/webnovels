# 3단계 — 인증·작가 가입·서버 권한과 API 기반

[전체 계획](improve.md) · 이전: [2단계](improve2.md) · 다음: [4단계](improve4.md)

## 목표와 선행 조건

2단계 격리 스키마와 계정 이관 계약을 기반으로 신규 작가가 실제 계정을 만들고 서버에서 본인으로 확인되도록 한다. 아직 미전환 기능을 운영에서 함께 열지 않는다.

## 대상 파일

- `server/secure-api.mjs`, `functions/api/v2/[[path]].js`, `functions/api/[[path]].js`.
- `public/js/reader/reader.js`의 가입/로그인, `public/js/core/state.js`, `router.js`.
- `public/supabase-admin.js`의 독자/작가/관리자 인증, `public/js/admin/admin.js` 로그인.
- 신규 제안: 공통 세션/API 클라이언트 모듈, 가입·권한 계약 테스트.

## 순차 작업

- [x] Supabase Auth를 로그인 신원의 기준으로 통일하고 토큰 갱신·만료·로그아웃 처리를 공통화한다.
- [x] Auth 가입, 이메일 확인, 필명 설정, 작가 프로필 생성의 실패/재시도를 정의한다. 계정만 생성된 경우 재개 가능하게 한다.
- [x] 기존 작가 연결·비밀번호 재설정·관리자 초기 연결을 검증된 절차로 제공한다.
- [x] 준비 중 안내로 끝나는 `handleAuthorSignup()`을 실제 서버 계약에 연결한다.
- [x] 역할 변경·정지·탈퇴는 서버의 현재 계정 상태로 판정한다. 로컬 역할/ID 또는 토큰 모양만으로 권한을 주지 않는다.
- [x] 서버에서 요청별 소유권·필드 허용 목록을 검사한다. 작가 변경 요청으로 역할·작성자·정산 상태를 수정하지 못하게 한다.
- [x] `/api/v2/me`와 공통 오류(401/403/409/검증 실패/503), 요청 식별자·재시도 규칙을 정한다.
- [x] 쓰기 요청의 Origin/CORS·세션 전달 방식과 남용 제한을 정하고, 쿠키를 사용한다면 CSRF 방어를 포함한다.
- [x] 브라우저 비밀번호 테이블 조회·가짜 토큰·자동 관리자 승격 경로를 대체한다. 다른 호출이 남으면 10단계 폐쇄 목록에 기록한다.
- [x] 개인정보 열을 최소화하고 서비스 키가 정적 파일·로그·공개 설정에 포함되지 않는지 검사한다.

## 검증과 완료 조건

- [ ] 신규 가입 → 이메일 확인 → 작가 프로필 생성 → 재로그인 → 비밀번호 재설정이 격리 환경에서 동작한다.
- [x] (합성 DB/VM) 가입 중 네트워크 실패를 재시도해도 계정/프로필이 중복되지 않는다.
- [x] (API mock) 익명, 독자, 작가 A/B, 제한/최고 관리자, 정지·만료 세션으로 권한 행렬을 검증한다.
- [x] (API mock/VM) 변조된 로컬 ID/역할로 타 작가 작업이나 관리자 API를 호출하면 거절된다.
- [x] (API mock/VM) Auth/DB 장애와 잘못된 비밀번호를 구분해 안전한 오류를 반환한다. 실패를 로그인 성공으로 처리하지 않는다.

## 복구와 외부 의존성

메일 발송·리디렉션 도메인·기존 Auth 매핑이 필요하다. 미설정이면 해당 검증은 미완료로 남긴다. 오류 시 새 가입/변경 경로를 제한하되 기존 계정을 삭제하거나 임의 비밀번호로 바꾸지 않는다.

## 실행 기록

- 상태: **로컬 구현·검증 완료 / 실환경 통합 검증 대기**
- 시작일 / 로컬 검증일: 2026-09-23 / 2026-09-23. 단계 전체 완료일은 미정.
- 변경: 공통 Auth/API 모듈, 이메일 확인 후 가입 재개·비밀번호 재설정 UI, 로그인/라우터 전환, 서버 가입 RPC·요청 ID·DB 횟수 제한, 004 SQL, 테스트·CI.
- 상세 계약·남은 폐쇄 경로·운영 절차: [auth-contract.md](docs/launch/auth-contract.md).
- 검증: API mock 25개, 세션 VM 7개, 합성 PostgreSQL 가입 4개 통과. 기존 authoring 22개·Cloudflare 4개·provider 7개·데이터 계약·tsc·JS 구문 검사 통과.
- 증거: [step3-auth-verification.json](artifacts/step3-auth-verification.json). 커밋·배포는 하지 않음.
- DB 적용: 합성 로컬 PostgreSQL만. 실제 Supabase에 004를 적용하거나 기존 Auth 계정을 변경하지 않음.
- 미해결: 2단계 실제 스키마/백업/매핑, 메일·redirect allowlist·Auth rate limit 설정, 실제 브라우저 가입/재설정 인수 검증.
- 다음 단계: 4단계 로컬 코드 준비 가능. 선행 외부 조건 해결 전 통합 완료·운영 오픈은 대기.
