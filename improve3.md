# [Improvement Step 3] 통합 검증 및 안정성 테스트 가이드

## 1. 검증 목표
- 1단계(계층적 Semantic URL 라우팅) 및 2단계(모듈화) 적용 후, 브라우저 주소창 직접 입력(Deep Linking), 새로고침, 브라우저 뒤로가기/앞으로가기 환경에서 시스템이 무결하게 작동하는지 검증합니다.

---

## 2. 핵심 검증 시나리오 (Test Scenarios)

### 시나리오 1: 독자 서비스 딥링크 검증
1. **회차 즉시 열람 (`/read/:workId/:epNum`)**:
   - 주소창에 `/read/1/2` 직접 입력 후 엔터.
   - [기대 결과] 1번 작품(대적자)의 제 2화 본문이 리더 뷰어(`view-reader`)에 즉시 렌더링되며, 헤더/하단 네비게이션이 뷰어 모드로 정렬됨.
2. **작품 상세 (`/works/:id`)**:
   - 주소창에 `/works/2` 직접 입력.
   - [기대 결과] 2번 작품(천마의 귀환) 상세 화면(`view-work-detail`)이 노출되고 회차 목록이 정상 출력됨.
3. **내 서재 탭 분기 (`/library/favorites`)**:
   - 주소창에 `/library/favorites` 직접 입력.
   - [기대 결과] 내 서재 뷰(`view-mypage`)로 진입하며 '관심 작품' 탭이 즉시 활성화됨.

### 시나리오 2: 작가센터 계층적 라우트 검증
1. **작가 회차등록 직행 (`/creator/episodes`)**:
   - 주소창에 `/creator/episodes` 직접 입력.
   - [기대 결과] 작가 스튜디오(`view-creator`) 진입과 동시에 2번 탭(신규 회차 등록 & AI Zero-Touch)이 활성화되며, 풀스크린 모드가 적용됨.
2. **작가 정산관리 직행 (`/creator/settlement`)**:
   - 주소창에 `/creator/settlement` 직접 입력.
   - [기대 결과] 7번 탭(정산관리)이 즉시 표출됨.

### 시나리오 3: 관리자 관제탑 권한 및 서브탭 검증
1. **회원 관리 직행 (`/admin/users`)**:
   - 미로그인 상태에서 `/admin/users` 접근 시: 관리자 로그인 모달 자동 팝업.
   - 로그인 완료 시: 02. 회원 관리 탭 즉시 활성화.
2. **콘텐츠 5대 검수 콘솔 직행 (`/admin/episodes`)**:
   - `/admin/episodes` 직접 접근 시: 05. 회차 및 검수 콘솔 탭 즉시 활성화.

### 시나리오 4: 브라우저 히스토리 (Popstate) 무결성
- 홈(`/`) → 작품 상세(`/works/1`) → 회차 뷰어(`/read/1/1`) → 브라우저 뒤로가기 버튼 클릭.
- [기대 결과] 페이지 새로고침 없이 부드럽게 `/works/1` 상세 화면으로 복원되며, 다시 뒤로가기 시 `/` 홈 화면으로 온전히 복원됨.

---

## 3. 코드 안정성 검증 기준
- Node.js 구문 검사: `node -c` 에러 0건.
- TypeScript 컴파일: `npx tsc --noEmit` 에러 0건.
- 브라우저 콘솔: Uncaught TypeError / 404 Not Found 에러 0건.
