# 🚀 WebNovels 및 일반 개발 가이드라인 (AI 어시스턴트 필수 참조)

이 파일은 `webnovels` 프로젝트는 물론, 다른 폴더에서도 유용하게 활용할 수 있는 프론트엔드-백엔드 데이터 동기화 및 DB 스키마 검증 원칙을 담고 있습니다. 코드를 작성하거나 디버깅할 때 다음 원칙을 반드시 준수하세요.

## 1. 프론트엔드: 맹목적인 데이터 덮어쓰기 방지 (로컬 보호 방어벽)
*   **증상:** 백엔드 업데이트 실패 또는 네트워크 지연으로 인해 백엔드가 빈 상태(Empty state, `[]`, `null`)를 반환할 때, 이 빈 데이터로 프론트엔드의 최신 로컬 데이터(로컬 스토리지 등)를 덮어쓰게 되면 사용자 데이터가 모두 초기화되는 치명적인 버그가 발생합니다.
*   **원칙:** 백엔드에서 데이터를 `fetch`하여 로컬 상태를 동기화할 때, **백엔드의 값이 비어 있고 로컬에 이미 데이터가 존재한다면 절대 덮어쓰지 말고 기존 로컬 데이터를 보존(SSOT 유지)**해야 합니다.
*   **검증:** 데이터 병합(Merge) 로직을 작성할 때는 빈 배열이나 null 값이 기존 데이터를 파괴하지 않는지 명시적으로 점검하세요.

## 2. 백엔드(Supabase 등): 테이블 스키마 및 RLS 권한 누락 체크
*   **증상:** 프론트엔드에서 코드를 올바르게 작성했음에도 백엔드에 데이터가 저장되지 않는 경우(Silent Fail), 대부분 DB 테이블의 **컬럼 누락**이거나 **RLS(Row Level Security) 정책** 차단 때문입니다.
*   **원칙:** `UPDATE` 또는 `INSERT` 코드를 작성하거나 리뷰할 때, 페이로드에 포함된 모든 필드(예: `reading_history`, `favorites`, `nickname` 등)가 **실제 DB 스키마에 존재하는지 반드시 교차 검증**해야 합니다.
*   **원칙:** 스키마가 존재하더라도 쓰기 권한이 막혀있을 수 있으므로, RLS 정책이 해당 컬럼의 변경을 허용하고 있는지 체크하고, 만약 누락이 의심된다면 사용자에게 **해결을 위한 SQL (ALTER TABLE 및 CREATE POLICY)**을 작성하여 Dashboard에서 실행할 수 있도록 적극적으로 안내하세요.

## 3. 적용 대상
*   이 규칙은 로컬 스토리지를 활용한 오프라인 폴백(Offline Fallback) 앱, 정적 배포된 SPA, Supabase/Firebase 등을 사용하는 서버리스 백엔드 연동 환경에서 **무조건 최우선으로 적용**됩니다.
